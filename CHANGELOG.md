# Changelog

All notable changes to this project are documented here (Keep a Changelog / SemVer).

形式は [Keep a Changelog](https://keepachangelog.com/)、バージョンは [SemVer](https://semver.org/lang/ja/)。
バージョンの正本は `src/apps/Amm/src-tauri/Cargo.toml` の `version`（`tauri.conf.json` と同期）、Git タグは `v<SemVer>` に一致させる。

## [Unreleased]

### Removed
- チャット記録・統計情報機能を削除（ペインメニュー項目・プロファイル設定・関連ファイル出力を含む、ユーザー要望）

### Changed
- ペインタイトルバーに「名前変更」「フォントサイズ」「エディタ連携」「エディタ連携ファイルパスをコピー」の
  アイコンボタンを追加し、⋮メニューの奥からタイトルバー直接のクリックへ移動

### Fixed
- 起動時、`profiles.amm` に定義のないコマンド（前回終了時に生存していたペインの数だけ、プロファイルに紐付かない
  素のシェル）が自動的に起動してしまう不具合を修正。原因はペインの並び順をブラウザ `localStorage` へ保存し次回
  起動時にその生存数だけ機械的に再生成していたことで、どのプロファイル/コマンドだったかは元々保存しておらず
  常に無関係な空シェルにしかならなかった。この経路を廃止し、起動時に生成するペインは各プロファイルの
  `autoStartCount` 設定のみに従うよう変更（再インストール等で `localStorage` だけが残っている場合に、
  コマンドが無断で自動起動して見えるとのユーザー報告）
- macOS でネイティブのフォルダ/ファイル選択ダイアログ（作業ディレクトリ選択・プロファイルの開く/名前を付けて
  保存/インポート・エクスポート・エディタパス選択等）を開くとアプリ全体がフリーズする不具合を修正
- 「上書き保存」実行後のステータス表示が常に固定文字列 `profiles.amm` を表示しており、実際の保存先
  （File>開く/名前を付けて保存でアクティブファイルを切り替えていた場合）と食い違う不具合を修正
- macOS で `.app` から起動すると launchd 経由で `TERM` 環境変数が渡らず、Claude Code 等のCLIが簡易表示に
  フォールバックしていた不具合を修正（未設定時のみ `xterm-256color` を補完）
- ペインの名前変更ダイアログが macOS/WKWebView 環境で `window.prompt()` が機能せず全く表示されない不具合を修正
  （`openModal` ベースの自作ダイアログへ置き換え）
- `window.confirm()` がバンドル済み `tauri-plugin-dialog` バージョンで対応する Rust コマンドが削除済みのため
  常に失敗し、確認なしで削除・上書き系の操作が進んでしまう不具合を修正（`openModal` ベースの自作確認ダイアログへ
  全9箇所を置き換え）
- ペイン右クリックメニューの「コピー」「クイック送信に登録...」が、Claude Code 等 CLI 自身の自動コピー機構に
  よりテキスト選択直後に xterm 内部の選択状態がクリアされるため常に無効化されて見える不具合を修正
- 「クイック送信に登録...」をテキスト未選択でも開けるよう仕様変更し、ダイアログに「直前のプロンプトを設定」
  「クリップボードテキストを設定」ボタンを追加
- ボタン以外の場所（空きデスク領域・ツールバーボタン等）で右クリックすると WKWebView のネイティブメニュー
  （「再読み込み」等）が表示されてしまう不具合を修正
- ボタンをマウスで長押しすると WKWebView 既定のテキスト選択コールアウトが表示されてしまう不具合を修正
- macOS 版の配布用既定プロファイル生成スクリプトが Windows 用 `profiles.amm` を誤って同梱していた不具合を修正
- コマンドタイプ「Antigravity CLI」の Windows 版起動パスが誤っていた（npm 経由の `%APPDATA%\npm\agy.cmd` を想定していたが、
  実際は専用インストーラで `%LOCALAPPDATA%\agy\bin\agy.exe` に配置され、既定でPATHにも追加される）不具合を修正。
  macOS/Unix 版と同様に実行ファイル名のみを指定しPATH解決に委ねる方式へ変更
- Windows 版で git-integration の内部処理（ペインクローズ時のコミット確認ガード等）が git コマンドを実行するたびに
  コンソールウィンドウが一瞬表示される（アプリ終了時等に連続実行され点滅して見える）不具合を修正
  （`CREATE_NO_WINDOW` フラグ未設定、`gateway.rs` 側の同種処理には既に設定済みだった）
- 「プロファイルに未保存の変更があります」終了時ダイアログの保存先が、File>開く等で明示的にファイルを
  切り替えていない場合に Windows ではアプリのインストール先（`Program Files\amm\`、通常書き込み不可）が
  既定になっていた不具合を修正。既定は起動時のカレントディレクトリとし、それがシステム管理下のディレクトリ
  （Windows: Program Files/Windows/ProgramData 等、macOS: /Applications 等）を指す場合はドキュメントフォルダに
  フォールバックするよう変更

### Changed
- 「コマンド追加」フローを、種類選択 → （作業ディレクトリ選択 →）名前・タイトルバー色入力の3画面構成から、
  作業ディレクトリ選択 → 種類・名前・タイトルバー色をまとめた1画面へ再構成
- コマンドタイプ「Gemini」を「Antigravity CLI」へ改称（gemini-cli が antigravity-cli へ統合されたため）
- 送信前のテキスト整形（連続空行をまとめる/コメント記号）を、コマンドごとの設定からアプリ全体設定へ変更し、
  適用範囲も共通入力欄・エディタ連携からの送信のみへ限定（クイック送信・プロンプト再送信・他CLIからの
  MCP経由送信・アイドル時自動送信には適用しない）
- クイック送信（右クリックメニュー登録済みプロンプト）を、コマンドごとの設定からアプリ全体・全ペイン共通の
  設定へ変更
- エディタ連携設定（使用するエディタ/カスタムエディタのパス/送信後アクション）を、ペイン単位に見える
  「AMM設定」ダイアログから「全般設定」ダイアログへ移動
- ペインメニューの「AMM設定」を「コマンド設定」へ改称（そのペインに紐づくコマンド固有設定のみを扱うため）
- ツールバーに新設の「設定▶」ボタン（連続空行/コメント記号・クイック送信・エディタ連携を管理する「全般設定」
  を開く）を追加し、既存の「設定▶」は「AI設定▶」へ改称（MCPゲートウェイ設定/CLIへのMCP登録はそのまま）
- 配布用既定プロファイル（`profiles.amm`/`profiles.macos.amm`）から既定の5コマンド（zsh/Claude Code/
  Copilot/Codex/Antigravity CLI）を削除し、空の状態から「コマンド追加」で利用者自身が追加する構成に変更
- `artifacts/` 配下の `publish`/`packages` をマルチプラットフォーム対応のため variant 別
  （`tauri-windows`/`tauri-macos`/`legacy`、将来の `tauri-linux` 用に予約名も用意）に再編
- CI/CD をレガシー.NET版からTauri版（Windows NSIS/MSI・macOS `.app`/`.dmg`）へ全面切替。`main` への push で
  バージョン（`tauri.conf.json`/`Cargo.toml`）に基づく下書きリリースを自動作成・更新し、`main` へのPR作成/更新で
  `vX.Y.Z-prN` タグのプレリリースを自動公開するよう `release.yml`/`pr-prerelease.yml` を再構成
- Windows NSISインストーラ（`.exe`）がOSのロケールに応じて英語/日本語のインストーラ画面を自動的に切り替えるように
  変更（`bundle.windows.nsis.languages`、これまでは未設定で英語固定だった）。MSI（`.msi`）は言語ごとに別ファイルを
  生成する方式のため対象外

### Fixed
- macOS配布用`.dmg`内の`amm.app`が実際には署名の整合性検証に失敗する状態（`codesign --verify`が
  「code has no resources but signature indicates they must be present」で失敗）で配布されており、
  ダウンロードして開こうとすると「"amm"は壊れているため開けません」と表示され起動できない不具合を修正。
  原因は`cargo tauri build`が`.dmg`を組み立てる時点で`.app`が未署名（署名IDが未設定のため
  tauri-bundler自身の署名処理がスキップされていた）で、ビルドスクリプト側の事後的な`codesign`は
  `.dmg`作成後の別コピーにしか効いていなかったこと。`tauri.conf.json`の`bundle.macOS.signingIdentity`を
  `"-"`に設定し、`.dmg`組み立て前に正しい順序でad-hoc署名されるようにして解消

## [1.2.0.0] - 2026-06-30

### Added
- 統計情報メニューをサブメニュー化（記録オンオフ + 表示をまとめて配置）
- ターミナル本体（WebView2）の右クリックメニューにもチャット記録・統計情報トグルを追加
- MDI 切り替えボタンにフォーカスがある状態でも Ctrl+S 等の送信ショートカットを有効化
- ログ・統計情報のファイル名先頭にコマンド名（プロファイル名）を付与
- 「新しい名前でコマンドを追加」画面でチャット記録・統計情報のオンオフを選択可能に

### Changed
- 統計情報 (Stats) の既定値を OFF → ON に変更

### Fixed
- ターミナルへ直接タイプして送信した場合にチャット記録・統計情報が発火せず `.amm`
  フォルダが作成されない不具合を修正

## [1.0.0] - 2026-06-23

v1.0.0.0 として新規リリース。
