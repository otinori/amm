# dashboard/ — ダッシュボード生成物

`/udr-dashboard` スキル等が生成する閲覧・共有用スナップショット（`udr_dashboard.html` 等）。

- **生成物**（再生成可）。手で編集しない。
- `udr_dashboard.html` は現状 `.gitignore` 対象（生成物のためコミットしない方針）。
  チームで履歴閲覧・共有したい場合は `.gitignore` から外してコミットする。
