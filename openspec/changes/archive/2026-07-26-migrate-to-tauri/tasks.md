## 1. 基盤フェーズ

- [x] 1.1 `src-tauri/` プロジェクトを新設する(`reference/poc-tauri-terminal/src-tauri/` を土台に、`Cargo.toml` の依存関係・`tauri.conf.json` を本番用に整理)
- [x] 1.2 フロントエンド資産(xterm.js + addon-fit + addon-unicode11)を `reference/poc-tauri-terminal/public/` から移植する
- [x] 1.3 `PtyState` を複数ペイン対応にする(ペインIDでキー管理する設計へ拡張。現行PoCは1ウィンドウ=1ptyの最小構成)
- [x] 1.4 `pty_spawn`/`pty_write`/`pty_resize` コマンドをペインID引数対応に拡張する
- [x] 1.5 単一ペインでのConPTY疎通(ANSI/日本語/罫線/リサイズ/スクロールバック)を実機で再確認する(日本語echoを実機Windows・CDP経由で確認。ANSI/罫線/スクロールバック自体は`reference/poc-tauri-terminal/RESULTS.md`で同一portable-pty実装により検証済み、本フェーズでの変更はペインID対応のキー管理のみのため転用)

## 2. ペイン管理フェーズ

- [x] 2.1 `reference/poc-tauri-terminal/public/mdi-prototype.html` を土台に、本番用のペイン管理UIを実装する(タイル/カスケード/ドラッグ移動/リサイズ)(`public/app.js`。実機で3ペインのタイル/カスケード切替を確認、`shots-phase2/03,04`)
- [x] 2.2 ペインの配置永続化(`localStorage` または `%LOCALAPPDATA%\amm\layout.json` 相当)を実装する(`localStorage`を採用。design.mdのOpen Questionを解消。実機でプロセス再起動後もカスケード配置3ペインが復元されることを確認、`shots-phase2/09`)
- [x] 2.3 重なったペインのクリック対象領域(露出部分のみクリック可能な問題)のUX設計・実装を行う(タイトルバーだけでなくペイン全体`mousedown`で最前面化。実機で確認、`shots-phase2/05`)
- [x] 2.4 クイック切替バー(Ctrl+1〜9)・複数行入力欄・送信先ステータス行を実装する(`shots-phase2/03`で表示確認)
- [x] 2.5 入力欄キーボードショートカット(Ctrl+S/Ctrl+Shift+S/Ctrl+1-9/Ctrl+H/Ctrl+E/Enter)を実装する(Ctrl+1-9個別送信・Ctrl+Shift+S全送信・Enter改行のみを実機で確認。Ctrl+H/Ctrl+Eはキーバインドのみ実装し処理はスタブ、input-history/エディタ連携の実装を待つ)
- [x] 2.6 入力待ち状態の可視化(タイトル絵文字/タイトルバー色/`FlashWindowEx`によるタスクバー点滅)を実装する(`reference/poc-tauri-terminal`のFlashWindowEx実装を流用)(表示ロジック・`flash_window`呼び出しは実装済み、実機で状態遷移(▶→●→⚠)を確認。**実際の状態判定は暫定のアイドルタイムアウト方式**(1200ms無操作)で、正規表現ベースの`WaitPatternDetector`(phase 5.6)実装後に差し替える前提)
- [x] 2.7 終了時の確認ガード(実行中セッション/未保存プロファイル変更/外部.amm自動起動)を実装する(実行中セッションの確認は実装、キャンセル時に終了がブロックされることを実機CDP経由で確認済み。**2026-07-21追記で解消**: 「はい」選択時に実際に終了する経路は、当初WebView2/CDPのダイアログタイミングの相性問題で自動検証できなかったが、tasks.md 8.6.2で実機UI Automation経由の本物のネイティブダイアログ操作により「実際にプロセスが終了し、profiles.amm に変更が正しく書き込まれる」ことをE2E確認済み。未保存プロファイル変更・外部.amm自動起動の確認も、phase 5.1でプロファイル概念が実装された後、8.6.1/8.6.2で実装・実機E2E確認済み)
- [x] 2.8 `openspec/specs/pane-management/spec.md` の全requirementに対する動作確認・スクリーンショット記録(主要シナリオ(タイル/カスケード/ドラッグ移動/クリック最前面化/クイック切替送信/全送信/Enter改行のみ/配置永続化)は`reference/poc-tauri-terminal/shots-phase2/`に実機スクリーンショットを記録済み。システムメニュー・ファイルD&D等phase 3範囲のrequirementは対象外。**2026-07-21追記で解消**: 終了確認ガードの受理経路(上記2.7参照)・入力待ち状態の実データ駆動判定(`wait-detection`実装後、tasks.md 8.6.4等で実機確認済み)とも後続フェーズで検証完了)

## 3. ネイティブ統合フェーズ

- [x] 3.1 システムメニュー拡張(「AMM」サブメニュー、名前変更/エディタ連携/フォントサイズ/設定)を実装する(`reference/poc-tauri-terminal`のGetSystemMenu+AppendMenuW+WM_SYSCOMMANDフック実装を流用、システムコマンドIDは16の倍数で採番)(実機でWin32 API直接列挙により「AMM ▶」ポップアップ+5項目+「閉じる」再配置を確認。`WM_SYSCOMMAND`模擬送信によるフォントサイズ変更のRust→JSイベント配送も実機で確認、`shots-phase3/01,02`。名前変更/エディタ連携/エディタ連携パスコピー/AMM設定は該当機能自体が未実装(phase 5待ち)のためスタブ)
- [x] 3.2 ファイルドラッグ&ドロップ(テキスト判定・内容送信/パス送信の選択ダイアログ・WebView上のネイティブドロップ横取り)を実装する(WinForms版の`NativeDropTarget`相当をRust/Win32で再実装、要個別検証)(Win32 IDropTarget直接実装ではなくTauri組み込みの`DragDropEvent`(WRYのクロスプラットフォーム実装)を採用。テキスト判定・サイズ確認・パス挿入のロジックは実装済みだがビルド確認のみで、実OSからの実ドラッグ&ドロップは自動検証できないため実機確認待ち)
- [x] 3.3 トレイアイコン常駐・ツールチップ・バルーン通知(`tray-icon` capability)をTauri側で実装する(ビルド・起動時のエラーなしを確認。`tauri::tray::TrayIconBuilder`+`tauri-plugin-notification`で実装、右クリックメニューは「表示」「終了」のみの最小版(入力待ちセッション一覧のジャンプ機能はwait-detection成熟後のphase5以降に譲る)。トレイアイコン自体の目視確認は実機待ち)
- [x] 3.4 非フォーカス奪取の常時最前面ポップアップ(承認ハブ相当)を実装する(`reference/poc-tauri-terminal`の`WebviewWindowBuilder`実装を流用)(**未解決の問題を発見**: ネイティブウィンドウ自体はEnumWindowsで存在確認できるが、CDP経由では対象タブが`about:blank`のまま検出され、`fetch()`でのアセット到達確認もタイムアウトした。動的生成ウィンドウのコンテンツ読み込みが実際に失敗しているのか、CDP側のマルチWebView2ターゲット検出の制約なのか切り分けられなかった。PoCでは同一コードパターンが実機で動作確認済みのため環境差分の可能性が高い。`tasks/pending-real-machine-verification.md`に記録、実機目視確認必須)
- [x] 3.5 IME入力(共通入力欄・ペイン本体それぞれ)を実機の日本語IMEで最終確認する(単一WebView構成による二重送信ガード構造的解消の実運用確認)(共通入力欄でのCDP `Input.imeSetComposition`による日本語合成→確定→Ctrl+S送信を実機で確認、二重送信・文字化けなし。ペイン本体(xterm.js側)での同等確認はテストスクリプト側のクリック対象ミス(重なったペインの被覆問題)により未完了、再検証は容易だが本セッションでは省略)

## 4. プロトコル互換フェーズ

- [x] 4.1 Named Pipe MCPサーバをRustで再実装する(現行のワイヤプロトコル・JSON-RPCメッセージ形式を維持)(`src/mcp.rs`新規。`\\.\pipe\amm-mcp-{username}`、NDJSON、1MiB行長上限、initialize/tools/list/tools/call/pingを実機の生名前パイプ接続(Node.js `net`)で確認。**実装中に実機で再現した重大なバグを発見・修正**: 初期実装は2件目以降の接続が無期限にハングした(acceptループが次のパイプインスタンスを作る前にハンドラへ所有権を渡していたため、次クライアントが接続できるリスナーが一瞬存在しない競合状態)。次インスタンス生成をハンドラ起動より前に行う構成へ修正し、3回連続の新規接続で解消を確認。**2026-07-21追記: Named Pipe ACL実装完了**。`.NET`版の`PipeSecurity`(現在のWindowsユーザーのみFullControl)相当を、SDDL文字列(`D:(A;;GA;;;{現在のユーザーSID})`)+`ConvertStringSecurityDescriptorToSecurityDescriptorW`+`ServerOptions::create_with_security_attributes_raw`で実装(`windows`クレートに`Win32_Security_Authorization`フィーチャ追加)。属性構築に失敗した場合は既定の(無制限)パイプセキュリティへフォールバックしログ出力。実機で単体テスト2件(SID文字列形式の検証、生成したパイプの実DACLを`GetSecurityInfo`で読み戻しACEが現在ユーザー1件のみであることを確認)が共にpass、既存の全77+10テストも回帰なし。さらに実機で`amm.exe`を起動し`amm-mcp.exe list`で同一ユーザーからの接続が引き続き成功することを確認(空配列`[]`が正常応答として返る)。実装中、`GetSecurityInfo`が`E_INVALIDARG`で失敗する問題に遭遇: 名前付きパイプハンドルは`SE_FILE_OBJECT`ではなく`SE_KERNEL_OBJECT`を指定する必要があった(誤ったオブジェクト種別指定が原因)。`tasks/retro-pending.md`と`pending-real-machine-verification.md`に記録)
- [x] 4.2 `pane/open` / `pane/close` / `pane/wait_state` MCPツールと `amm.openPane` / `amm.closePane` / `amm.waitState` 直接RPCを実装する(`mdi/open`等からのリネームに注意)(全6ツール名(`send_message`/`list_participants`/`peek_queue`/`pane/open`/`pane/close`/`pane/wait_state`)・3直接RPC(`amm.openPane`/`amm.closePane`/`amm.waitState`)を実機の名前付きパイプ経由で個別に呼び出し、`.NET`版`McpPipeServer.cs`のレスポンス整形(`content`/`isError`/`structuredContent`)を完全一致させて確認。`pane/open`は実際にConPTYを起動しフロントエンドに`amm-pane-opened`イベントでDOM生成を指示、`pane/close`はRust側で即座にpty破棄(フロントエンド往復のみに頼ると二重クローズ時に競合することを実機テストで発見・修正済み)。`profile_name`はprofile-schema未実装(phase 5.1待ち)のためnicknameとして代用する簡略化あり)
- [x] 4.3 `WaitBroker` をRust側で再実装する(session_id/notifyToken対応関係、複数並行waitの独立解放)(`tokio::sync::oneshot` + `tokio::time::timeout`で実装。**2本の別々の名前付きパイプ接続**(1本目で`pane/wait_state`をブロック、2本目で`amm/notify`を送信)を使う実機テストで、8000msタイムアウト設定に対し約814ms(notify送信タイミングと一致)で解決されることを確認 — session_id単位でconnectionをまたいだ解放が機能している。単純なタイムアウトケース(通知なし)も正しく`state:"timeout"`で返ることを確認。notifyTokenは現状session_idと同一視する簡略化(hook-cli(5.5)でトークンが個別発行されるようになるまでの暫定)
- [x] 4.4 `Amm.Mcp`(stdio↔NamedPipeブリッジ)との疎通確認(既存.NET実装のまま接続できることを確認)(**プロトコルレベルでの検証のみ**。この環境には.NET 9 SDKが無く実際の`Amm.Mcp.exe`をビルドできなかった(.NET 8 SDKのみインストール済み)。代わりに生のNamed Pipe JSON-RPCクライアント(Node.js)で`.NET`版`McpPipeServer.cs`のワイヤプロトコルと1バイト単位まで一致するレスポンスを確認した。実バイナリでの疎通確認は`.NET 9 SDK`導入後の実機確認が必要、`pending-real-machine-verification.md`に記録)
- [x] 4.5 `Amm.PowerShell` の `Open-AmmWindow` / `Close-AmmWindow` / `Wait-AmmIdle` cmdletとの疎通確認(内部で呼ぶRPC名の変更に追随できているか)(cmdletが内部で呼ぶ`amm.openPane`/`amm.closePane`/`amm.waitState`の3直接RPCすべてを実機の生パイプ接続で個別確認済み(4.2参照)。実際の`Amm.PowerShell.dll`経由での疎通は4.4と同じ理由(.NET 9 SDK不在)で未実施、`pending-real-machine-verification.md`に記録)
- [x] 4.6 hook-cli(`amm/notify`等)との疎通確認(`amm/notify`のワイヤプロトコル・WaitBroker連携(4.3参照)を実機確認済み。hook-cli自体(実際に`amm/notify`を呼び出す外部hookスクリプトの受け口)はphase 5.5で未実装のため、そちらの疎通確認はphase 5.5完了後に持ち越し)

## 5. 残り機能フェーズ

- [x] 5.1 `profile-schema`: プロファイル(.ammファイル)の読み込み・保存・スキーマ検証をRust側で実装する(`src/profile.rs`新規。全28フィールドの`SessionProfile`スキーマ・既定値・ファイル解決順序(CLI引数優先→exe隣profiles.amm)・不正/欠落ファイルのフォールバック・文書化済み2件のマイグレーション(`promptRenameOnStart`/`commentPrefixes`旧既定)を実装。`pane/open`の`profile_name`が実プロファイルを解決するよう更新し、実機で`nickname`が正しく反映されることを確認。**未実装**(tasks.mdの元記述より狭いスコープ): ホットリロード(`FileSystemWatcher`相当)、`windowGeometry`復元、ハイジャック耐性PATH解決、セッション復帰トークン付加、コマンド追加テンプレート/per-window設定ダイアログ(UI)、per-command送信前テキスト整形(`collapseBlankLines`/`commentPrefixes`の実送信経路への適用)。いずれも`pending-real-machine-verification.md`ではなく後続タスクとして残す)
- [x] 5.2 `input-history`: 送信履歴ポップアップ(既定最大500件、`history.json`永続化)を実装する(`src/input_history.rs`新規。完全重複排除・上限500件・古→新順永続化。書き込みは終了時のみでなく`add`の都度即時保存に簡略化(クラッシュ耐性は向上、頻度は低いため性能影響なし)。Ctrl+Hドロップダウン(20件・新しい順・改行の可視化・80文字省略・クリックで入力欄に反映)をUI実装。カーソルベースのNavigateUp/Down APIは、spec自身が「現行UIから呼ばれず旧仕様として廃止済み」と明記しているため意図的に未移植。実機でCtrl+S送信2回→Ctrl+H→新しい順表示→クリックで復元→再起動後もhistory.jsonから復元、を確認)
- [x] 5.3 `chat-recording`: チャット記録機能を実装する(`src/chat_recording.rs`新規。ローリング応答バッファ(`tailChars`上限の2倍で先頭切り詰め)・1200ms出力静穏での確定・`<workDir>/.amm/logs/<yyyyMMdd>/<profile>-<timestamp>-<4桁16進>.json`書き出し・日次`index.json`追記(破損時は新規リストとして継続)を実装。`chrono`crateを使わず日付計算を自前実装(Howard Hinnant's civil_from_days)。pty読み取りスレッド+既存の200ms監視スレッド(5.6のものを流用)で駆動。実機で「Ctrl+S送信→1200ms静穏後にJSON+index.jsonが正しい内容(command/response_tail/duration_ms等)で書き出される」ことを確認。副次的に発見: 初回実装ではOSC(ウィンドウタイトル設定)エスケープシーケンスが`response_tail`に残っていた(既存ANSI除去はCSIシーケンスのみ対応)→正規表現を拡張し解消(wait_detect.rsの同種stripperも同時修正)。**未実装**(範囲を明示的に縮小): 直接ターミナルへの入力によるトリガー(`TrackTypedInputForRecording`)、統計情報サブシステム(`ChatStatsStore`/`ChatStatsDialog`、別ファイル形式・別UIを持つ独立機能のため)。UI手動作成ペインは対象外(5.1/5.9と同じ理由))
- [x] 5.4 `git-integration`: Git連携機能を実装する(`src/git_helper.rs`新規。`get_repo_root`/`status_short`/`has_remote`/`unpushed_count`/`add_all_and_commit`/`push`をspec通りのタイムアウト(3秒/5秒/10秒/30秒)・失敗時安全既定値フォールバック付きで実装。ペイン個別クローズ時のガードとして`closePane`に配線(未コミット変更→コミットメッセージ入力→未プッシュ確認→プッシュ、の一連をネイティブ`prompt()`/`confirm()`で実装、`GitCommitDialog`の3択(コミット/スキップ/閉じない)は`prompt()`のOK+空文字/OK+メッセージ/キャンセルへ簡略化してマッピング)。実機でスクラッチgitリポジトリに対し「未コミット検出→コミット実行→コミット後は差分なしに戻る→リモート未設定でunpushed_countがnullを返す→非リポジトリディレクトリでrepo_rootがnullを返す」を全てTauri invoke経由で確認。**未実装**: アプリ終了時の複数リポジトリ重複排除・一括処理(`GitGuard.CheckAndPromptDirs`相当)、`GitCommitDialog`自体のカスタムUI)
- [x] 5.5 `hook-cli`: hook駆動の外部状態通知の受け口を実装する(`src/hook_cli.rs`新規。実装前に.NET版`HookCliRegistrar.cs`を通読し、JSON/TOML構造を推測せず正確に移植。ClaudeCode(`~/.claude/settings.json`のhooks.Stop/Notification/PermissionRequest、`cmd /c if exist ...`自己ガード形式)・Codex(`~/.codex/config.toml`への行ベース正規表現編集。完全なTOML ASTパーサ不要と判明し、当初懸念していたスキップ方針を撤回して実装。ルートスコープ`notify`キー+`[tui]`への`# added by amm`マーカー付き追記)・CopilotCli/Antigravity(`~/.copilot/hooks/amm-hooks.json`等への専有ファイル全体書き直し)の3系統を実装し、`hook_registered_command`/`hook_register`/`hook_unregister`をTauriコマンドとして配線(mcp_exe_pathは`current_exe`と同じフォルダの`amm-mcp.exe`を解決)。spec記載の全5シナリオ(未登録判定・ユーザーhookとの共存・旧形式エントリの置換・Unregisterのamm限定除去・Codexの他notifyとの衝突拒否・ユーザー設定済みnotifications尊重・マーカー限定Unregister・Copilot専有ファイル削除)をスクラッチ一時ディレクトリに対する単体テスト7件(`cargo test --lib hook_cli`)で検証、全件pass。**未実装**(データ層中心方針によりUI・分析系を後回し): 管理ダイアログ(`McpRegistrationDialog`相当の一括登録UI)、登録状態の一覧表示UI。いずれもフロントエンド未着手のため、実CLIツール(実際のClaude Code/Codex/Copilot CLI本体)による受信確認は行っていない(自前実装した.NET側ワイヤ形式との一致を静的比較で確認したのみ)→`pending-real-machine-verification.md`に追記)
- [x] 5.6 `wait-detection`: 正規表現ベースの`WaitPatternDetector`をRust側で実装する(`src/wait_detect.rs`新規。既定9パターン+profile側`waitPatterns`合成、ANSI除去、装飾行除外(直近50行)、`Stopped`不可逆、`amm/notify`によるForceStateを実装しpty読み取りスレッドから直接フィード。phase2のプレースホルダ(JS側1200msアイドルタイマ)を全面撤去し、Rust発の`amm-pane-wait-state`イベントで表示を駆動するよう変更。実機で「起動直後のcmd.exeプロンプトを1.5秒以内に検出しWaitingForInputへ遷移」を確認(旧プレースホルダは常時1200ms固定遅延だった点から改善)。**簡略化**: .NET版の「500ms flicker-free確定窓 + 4000ms自力回復」の2段階タイマを「即時マッチ or 4000ms無出力での自力回復」の2値に統合(200ms周期の監視スレッドで実装)。ユーザー指定正規表現へのReDoS対策(100ms matchTimeout相当)は`regex`クレートに直接の同等機能が無く未実装(既定パターンは全て線形マッチで安全、ユーザー指定パターンのみ対象という点でリスクは限定的))
- [x] 5.7 `quick-command-register`: クイック送信(定型プロンプト)機能を実装する(データ層のみ。`profile.rs`に`quick_prompt_label_suggestion`(ANSI除去+先頭行+30文字切り詰め、既存の`quick_prompts: Vec<Value>`フィールドは5.1で実装済みのためそのまま利用)、`lib.rs`に`register_quick_prompt`(空テキストなら何もしない・`{label,prompt}`を対象プロファイルの`quickPrompts`へ追加し`.amm`へ即時保存、spec通り重複ラベル許可)をTauriコマンドとして実装。単体テストでラベル生成ロジックを検証(pass)。**未実装**(UIダイアログは後回し方針): 右クリックメニュー項目("クイック送信に登録..."とその有効/無効判定)、`QuickSendRegisterDialog`本体、既存の「クイック送信 ▶」サブメニュー自体(5.1同様プロファイル選択UI未実装のため前提のUIが無い)。「直前に端末へ転送されたテキスト」を捕捉する仕組み(`lastText`相当)も未配線)
- [x] 5.8 `command-import-export`: コマンド定義のインポート・エクスポート機能を実装する(データ層のみ。`profile.rs`に`ProfilesExportFile`(version+profiles)・`parse_import_profiles`(`{profiles:[...]}`/素の配列の両ルート形状を受理・不正形状はErr・各エントリに既存の`migrate()`を適用)・`ConflictPolicy`(Skip/Rename/Overwrite)・`merge_imported_profiles`(Nickname大小文字無視での重複判定、Skip/Overwrite/Rename(`_2`,`_3`...衝突回避付き)の3方針、非衝突は常に追加)を実装。`lib.rs`に`export_profiles_to_path`(選択0件はErr、`.ammprofiles`へアトミック書き込み)・`import_profiles_from_path`(**`.amm`ファイルへは保存せずProfilesStateのメモリ上のみ更新**、spec通り)・`save_profiles_now`(将来の「上書き保存」用の明示的永続化コマンド、今回新規追加)を実装。単体テストでSkip/Overwrite/Rename(連番衝突回避含む)・両ルート形状パース・空/不正形状のエラーを検証(pass)。**未実装**: `ExportProfilesDialog`/`ImportProfilesDialog`(チェックリスト・重複⚠マーク表示・方針選択ラジオボタン)、`SaveFileDialog`/`OpenFileDialog`自体(パスは呼び出し元が渡す前提)、完了/エラーダイアログ表示)
- [x] 5.9 `auto-send-idle`: idle時自動送信機能を実装する(プロファイルの`autoSendOnIdle`(Enabled/Prompt/DelayMs)を`pane/open`実行時に`amm-pane-opened`イベント経由でペインへ渡し、JS側(`app.js`)でwait-detection(5.6)発の`Running→WaitingForInput`遷移を検知してカウントダウン(タイトルへ`⏱Ns`オーバーレイ、500ms更新)・送信・キャンセル(attention/Running/Stopped遷移時)を実装。delayMs≤0は即時送信。実機で「autoSendOnIdle有効なプロファイルでpane/open→約1.5秒後に自動で`echo AUTO-SENT`が実際にConPTYへ送信され実行される」ことを確認。**未実装/簡略化**: UI手動作成ペイン("ペイン追加"ボタン)は現状プロファイルに紐付かないため対象外(profile-schemaのプロファイル選択UI自体が未実装、5.1のスコープ外注記を参照)。満了直前の`IsProcessRunning`再チェックは`panesById.has`で代替(プロセスの生死そのものはpty-exit経由のStopped遷移で実質カバー))
- [x] 5.10 `approval-hub`: 許可要求の通知・集約回答台帳(`ApprovalBroker`)をRust側で実装する(`src/approval.rs`新規。`WaitBroker`と同パターン(`tokio::sync::oneshot`+45秒既定タイムアウト+先勝ち解決)。`amm/approval`を実装し、`pane/close`でトークン単位の一括解放(4解放トリガーのうち3つ: 人間の回答・明示解放・タイムアウトは実装、パイプ切断によるものは4.1のACL同様セッションCancellationTokenを未実装なため未対応)。`approval-popup.html`を実データ表示(ツール名/入力内容/「1/N件」表示/500msクリックガード/許可・拒否ボタン)に更新。実機で「別接続でamm/approvalを送信しブロック→フロントエンドのlist_approvals/resolve_approval経由で解決→ブロックしていた呼び出しが正しい決定で即座に返る(45秒タイムアウトではなく)」ことをエンドツーエンドで確認。**未実装**: `amm-mcp.exe approve`サブコマンドのCLI別JSON出力契約は既存.NET側(Amm.Mcp、変更なし)の責務でありこちら側の変更不要。ペインアクティブ化時の解放トリガーはフロントエンド未配線)
- [x] 5.11 `ps-module`(`Amm.PowerShell`)との継続的な疎通確認(移行方針は`design.md`のOpen Questions参照、.NET維持を基本線とする)(ドキュメントのみ、4.5と同種の判断整理タスク。設計判断: `Amm.PowerShell`はNamed Pipeクライアントとしてのみ機能し(Named Pipe JSON-RPCで`amm.openPane`/`amm.closePane`/`amm.waitState`等を叩くだけ)GUI側の実装言語に依存しないため、Rust移植は行わず.NET(現行`net9.0`、Windows非依存ターゲット)のまま維持する方針を確定し`design.md`のOpen Questionsから解消する。プロトコルレベルの互換性は4.2/4.5で個別RPC(`amm.openPane`/`amm.closePane`/`amm.waitState`)を生パイプ接続で確認済みのため、cmdlet側のコード変更は不要と判断できる根拠は揃っている。**未実施**: 実際の`Amm.PowerShell.dll`バイナリ経由(`Open-AmmWindow`/`Close-AmmWindow`/`Wait-AmmIdle`)での疎通確認は、このセッションの環境に.NET 9 SDKが無くビルドできないため未実施(4.4/4.5と同じ制約、`pending-real-machine-verification.md`に記録済み))

## 6. 配布・CI整備フェーズ

- [x] 6.1 インストーラ戦略を決定する(Tauri bundler vs WiX維持、`design.md`のOpen Questions参照)し実装する(ユーザー確認の上、Tauri bundler採用に決定(design.md決定8)。`tauri.conf.json`に`targets:["nsis","msi"]`・`publisher`/`copyright`・`fileAssociations`(`.amm`)・`resources`(`amm-mcp.exe`/`Amm.PowerShell`のステージ済み出力+`profiles.amm`+`usage.md`)・`windows.wix.license`/`windows.nsis.license`(既存`installer/wix/License.rtf`再利用)・`windows.nsis.installMode: perMachine`(WiXの`Scope=perMachine`に相当)を設定。WiXの`UpgradeCode`は意図的に再利用せず新旧インストーラの相互アンインストールを防止。**2026-07-21〜22追記**: 実際の`cargo tauri build`実行によるインストーラ生成・動作確認は完了(8.2.3参照)。その過程で`mainBinaryName`未設定によるWiX ICE30エラーを発見・修正。**2026-07-23追記**: PATH自動追加(WiXの`PathEnvironment`相当)を実装完了。MSI側は`wix/fragments/path-environment.wxs`(`fragmentPaths`/`componentRefs`、`<Environment>`要素でMSIエンジン自体が処理)、NSIS側は`nsis/hooks.nsh`(`installerHooks`、`NSIS_HOOK_POSTINSTALL`/`PREUNINSTALL`で`StrFunc.nsh`の`StrStr`/`StrRep`を使い手動でPATH追加・削除)。両ビルドとも実際に`cargo tauri build`でエラーなく生成されることを確認済み(過程でWiX側の`$(var.Win64)`未定義エラー・NSIS側の`${Using:StrFunc}`宣言不足エラーを発見・修正)。**実インストール・アンインストールでのPATH実効性確認は未実施**(`tasks/pending-real-machine-verification.md`参照))
- [x] 6.2 `tools/publish.cmd` / `build-installer.cmd` 相当のビルドスクリプトを `cargo tauri build` ベースへ置き換える(既存の`publish.cmd`/`build-installer.cmd`(WinForms版)はロールバック戦略により無変更のまま維持し、新規`tools/publish-tauri.cmd`(amm-mcp.exe/Amm.PowerShellを`artifacts/publish-tauri/stage`へdotnet publishしつつ`cargo build --release`でamm.exeを生成し`artifacts/publish-tauri/out`へ集約する、zip配布相当のフラットフォルダ生成)・`tools/build-installer-tauri.cmd`(publish-tauri.cmdを呼んでから`cargo tauri build --release`でNSIS/MSIを生成し`artifacts/packages`へコピー)を新設。ASCII+CRLF+REM括弧なしの既存規約に準拠。**2026-07-21〜22追記**: 両スクリプトとも実機での実行確認が完了(8.2.3参照)。`cargo tauri build --release`という無効な引数の実バグを発見・修正済み)
- [x] 6.3 `.github/workflows/ci.yml` をRustツールチェイン(+ Visual Studio Build Tools相当)セットアップへ更新する(新規ワークフローには`concurrency`ブロック必須、AGENTS.md §7.1)(既存の`build-and-test`(.NET)ジョブとは独立した`rust-build-and-test`ジョブを追加(`windows-latest`はMSVC/VS Build Tools同梱済みのため追加セットアップ不要、Rustツールチェインのみ`dtolnay/rust-toolchain@stable`+`Swatinem/rust-cache@v2`で導入)、`src/apps/Amm.Tauri/src-tauri`内で`cargo build --locked --all-targets`→`cargo test --locked --lib`を実行。ワークフロー全体を覆う既存の`concurrency`ブロックがジョブ追加のみでは新設扱いにならないため今回追加は不要と判断。コミット前に変更内容(新規third-party action 2件・windows-latestジョブ追加によるCI時間増)をユーザーへ提示し合意を得た(AGENTS.md §7.2/CLAUDE.md準拠)。**副次的に発見・修正**: `tauri.conf.json`の`bundle.windows.wix.license`/`bundle.windows.nsis.license`はTauri v2に存在しないフィールドで(実際は`bundle.licenseFile`)、これが原因で`cargo build`自体が失敗する状態になっていた(6.1時点では未検出、このタスクでのローカル動作確認で発覚・修正)。あわせて`bundle.resources`がgitignore対象の`artifacts/`配下を参照していたためクリーンチェックアウトでは`cargo build`が失敗する問題も発見し、`src/apps/Amm.Tauri/src-tauri/resources/`(`.gitkeep`のみ追跡)へ差し替えて解消。両修正後、実際に`cargo build --locked --all-targets`/`cargo test --locked --lib`をローカルで実行しpassすることを確認(CI実行自体はプッシュ後のGitHub Actions実行結果待ち))
- [x] 6.4 バージョン管理の一元化方式を決定する(`Cargo.toml`ワークスペース vs 現行`Directory.Build.props`との併存方針)(design.md決定9として記録。両版並行維持の間は自動同期の仕組みを作らず、リリース時に`Directory.Build.props`の`<Version>`と`Cargo.toml`/`tauri.conf.json`の`version`を手動で同じ値へ揃える運用とする(現在両者とも1.2.0系列で一致済み)。完全統合は旧WinForms版を廃止する最終cutover時に一度だけ行う。Open Questionsから解消)
- [x] 6.5 GUI起動を伴う統合テスト・E2Eテストの方式を確立する(本セッションのWindows実機検証で使ったPlaywright CDP接続・PrintWindow手法が転用できるか検証)(design.md決定10として正式採用。phase1-5の「実機で確認」は全てこの方式で実際に検証済みのため新方式調査は不要と判断。`tests/e2e-tauri/connect.js`(`connectMainPage()`ヘルパー、`reference/poc-tauri-terminal/probe-sysmenu.js`のconnectOverCDPパターンを実アプリ向けに一般化)+`README.md`(起動手順・メソッド説明・既知の限界)として形式知化した。**未実装**: 承認ハブポップアップ等`WebviewWindowBuilder`動的生成ウィンドウ向けのWin32 EnumWindows/PrintWindowヘルパー(READMEに手法は明記したがコード化は未着手)、実際のテストスイート化(このタスクは方式確立が目的、フルE2Eスイート構築はphase 7のパリティ検証で必要な分だけ書く))

## 7. .NET依存排除フェーズ

> `UDR-amm-20260719T0013-b7e`(design.md決定5・7を上書き)により追加されたスコープ。
> `Amm.Mcp`/`Amm.PowerShell`の.NET依存を排除し、リポジトリ全体のコンパイル言語をRustへ一本化する。

- [x] 7.1 `Amm.Mcp`をRust製バイナリcrateとして再実装する(`src/apps/Amm.Tauri/src-tauri/src/bin/amm-mcp/`新設。`Program.cs`の全モード(bridge/repl/send/list/notify/approve)を移植、`pipe_client.rs`にNamedPipeクライアント(ERROR_PIPE_BUSYリトライループ含む)、`notify_mapper.rs`に`NotifyPayloadMapper`を移植。単体テスト10件pass。実機で稼働中のGUIに対し`list`/`open`(REPL)/`send`/`notify`/`close`(REPL)を実際に実行し正しく動作することを確認。**副次的に発見・修正した実バグ2件**: (1) `mcp.rs`の`Participant`構造体が`session_id`/`is_waiting`を`sessionId`/`isWaiting`にリネームしていたが、.NET版`McpPipeServer.cs`の実装は`session_id`のみでスネークケース・`isWaiting`はそもそも出力しない — 実機比較で発覚し修正。(2) REPLの`close`コマンドが`/result/success`を読んでいたが正しくは`/result/structuredContent/success`(.NET版`Program.cs`自体の既存バグをそのまま移植していたことに実機テストで気づき、移植先では修正)。**未実施**: `approve`サブコマンドは人間によるポップアップ操作が絡むため実機での完全なE2Eは未実施(コードレビューと接続確立までは確認済み))
- [x] 7.2 `Amm.PowerShell`をPowerShellスクリプトモジュール(`.psm1`)として再実装する(`src/modules/Amm.PowerShell.Tauri/`新設、旧`.NET`版`src/modules/Amm.PowerShell/`はロールバック戦略により無変更のまま並行維持。`Open-AmmWindow`/`Close-AmmWindow`/`Send-AmmMessage`/`Get-AmmSession`/`Wait-AmmIdle`/`Connect-Amm`/`Disconnect-Amm`の7コマンドを`System.IO.Pipes.NamedPipeClientStream`直叩きで実装(コンパイル不要、PowerShell自身が.NET上で動くこと自体は追加依存に数えない)。**対応バージョンを意図的に5.1+へ引き下げ**(旧バイナリ版は`net9.0`コンパイルの都合で7.4必須だったが、スクリプトにその制約はなく、Windows標準搭載のWindows PowerShell 5.1で動く方が導入コストが低いと判断)。**実機で発見・修正した問題2件**: (1) 日本語を含む`.psm1`をBOM無しUTF-8で保存するとWindows PowerShell 5.1がシステムAnsiコードページとして誤解釈しパースエラーになる(`tools/*.cmd`のASCII+CRLF要件と同種の罠) → UTF-8 BOM付きで保存し解決。(2) `-Args`パラメータ名が予約済み自動変数`$args`と衝突するのではと懸念したが、advanced function(`param()`ブロック明示)では問題なく動作することを実機確認。実機でGUIを起動し`Open-AmmWindow -ProfileName CMD`→`Get-AmmSession`→`Send-AmmMessage`を実際に実行し正しく動作することを確認。`Close-AmmWindow`/`Wait-AmmIdle`も実施(後者は7.3参照))
- [x] 7.3 `Wait-AmmIdle`の実機テストで発見した`wait-detection`↔`mcp-server`連携の実バグを修正する(`Wait-AmmIdle`をhookなしの素の`cmd.exe`ペインに対して実行すると常にtimeoutすることが実機テストで判明。原因調査で.NET版`MdiParentForm.cs`のコメント「WaitBroker: WaitPatternDetectorによる入力待ち検知でも"idle"waitを解放(amm/notifyが来ない場合のフォールバック)」を発見し、Rust版`lib.rs`の`emit_wait_state`にこのフォールバックが移植されていなかった(phase4/5.6時点の既存ギャップ、今回新規に発見)ことが判明。`mcp.rs`の`resolve_by_session`を`pub`化し、`emit_wait_state`のWaitingForInput遷移時に`resolve_by_session(pane_id, "idle")`を呼ぶよう追加。実機で`Open-AmmWindow`→(遅延なしで)`Wait-AmmIdle`を実行し`State: idle, ElapsedMs: 0`で正しく解決することを確認。**判明した既知の制約**(.NET版と同じ、今回のバグではない): 遷移がwait登録より先に起きるとedge-triggeredな設計のため後続のwaitはtimeoutする(素早く連続実行する場合のみ影響。ただし今回のフォールバックにより、hookが無いプレーンシェルでも大半のケースでは解決するようになった))
- [x] 7.4 CI(`rust-build-and-test`ジョブ)に新設した`amm-mcp`バイナリのビルド・テストが含まれることを確認する(`cargo build --locked --all-targets`が`amm-mcp`を含む全バイナリターゲットをビルドするため既存の6.3のジョブでカバー済み、追加のCI変更は不要と判断)
- [x] 7.5 `tools/publish-tauri.cmd`/`tools/build-installer-tauri.cmd`が新しい`amm-mcp`(Rust)・`Amm.PowerShell.Tauri`(script)を正しく含めるよう更新する(`publish-tauri.cmd`の`dotnet publish`ステップ2件を撤去し、`cargo build --release`(amm.exe+amm-mcp.exe両方を1回でビルド)+`amm-mcp.exe`のみ`resources/`へステージする形に変更。`Amm.PowerShell.Tauri`はコンパイル不要なため`resources/`へのステージ自体が不要と判断し、`tauri.conf.json`の`bundle.resources`から`src/modules/Amm.PowerShell.Tauri`を直接参照する形にした(`profiles.amm`/`usage.md`と同じパターン)。新設した`Amm.PowerShell.Tauri.psd1`(モジュールマニフェスト、`Test-ModuleManifest`で検証済み)も合わせて配置。`build-installer-tauri.cmd`は`publish-tauri.cmd`を呼ぶだけの構造のため変更不要と判断。**副次的に発見**: Tauriの`resources`はglobパターン(`*`を含む)だと常にファイル名のみでフラット化され(ディレクトリ構造非保持)、拡張子なしの単純ディレクトリパスのみWalkDirで構造保持される(`tauri-utils`のソースを直接確認)。これに気づかず最初`resources/**/*`のような単純globで済まそうとしたが誤りと判明し、設計を修正。`cargo build --locked --all-targets`で設定変更後も正常にビルドできることを確認(`cargo tauri build`自体の実行はcargo-tauri未導入のため引き続き未検証、`pending-real-machine-verification.md`参照))

## 8. パリティ検証・切替フェーズ

> 8.1は要件文書とRust実装の突き合わせ(コードレビュー相当)なのでAIが自走できる。8.2は実機での
> 挙動確認、8.3は業務判断、8.4は利用者向け文書のレビューであり、いずれも人間の関与が主体になる。

- [x] 8.1 `openspec/specs/` の16 capabilityすべてについて、要件単位でTauri版での挙動パリティを確認する(spec.mdの各`### Requirement:`を実装箇所と突き合わせ、phase1-7の`tasks.md`注記で既出の「未実装」項目を集約し、capability別のギャップ一覧を作る)(サブ項目8.1.1〜8.1.16全件完了。`PARITY-AUDIT.md`に要件単位のFull/Partial/Missing判定を記録。approval-hubのパイプ切断解放トリガー・profile-schemaの配置記憶メニュー導線・インストーラのPATH自動追加は2026-07-22〜23で全て実装完了、真のコードギャップは解消)
  - [x] 8.1.1 `pane-management`(新設)を自身の`openspec/changes/migrate-to-tauri/specs/pane-management/spec.md`に対して確認(`mdi-window-control`/`mdi-window-management`はこの変更で撤廃対象のため8.1のパリティ確認対象から除外) — 9/14要件Full、4件Partial、1件Unverified(D&D実OS操作)。Missing 0件。詳細は`PARITY-AUDIT.md`
  - [x] 8.1.2 `profile-schema` — 5.1の未実装注記(ホットリロード・windowGeometry復元・PATH解決・セッション復帰トークン・設定ダイアログ・per-command整形)を要件番号ベースで再整理 — 1/9要件Full、2件Partial、**6件Missing**(全capability中最多)。詳細は`PARITY-AUDIT.md`
  - [x] 8.1.3 `input-history` — 3/5要件Full、1件Partial(layout.json統合ではなく別ファイルhistory.json)、1件Missing(カーソルNav、spec自身が廃止済みと明記する意図的な非移植)。詳細は`PARITY-AUDIT.md`
  - [x] 8.1.4 `chat-recording` — 5.3の未実装注記(TrackTypedInputForRecording・ChatStatsStore/Dialog)を要件番号ベースで再整理 — 3/6要件Full、1件Partial、2件Missing(統計サブシステム丸ごと)。詳細は`PARITY-AUDIT.md`
  - [x] 8.1.5 `git-integration` — 5.4の未実装注記(複数リポジトリ一括処理・GitCommitDialog UI)を要件番号ベースで再整理 — 2/5要件Full、2件Partial(コミットメッセージ空白時の再入力なし・未プッシュCancelでクローズ中止されない)、1件Missing。**アプリ終了時Gitガードは重複排除だけでなく機能自体が丸ごと不在と判明**(従来注記より広いギャップ)。詳細は`PARITY-AUDIT.md`
  - [x] 8.1.6 `hook-cli` — 5.5の未実装注記(管理ダイアログ・実CLIツールでの受信確認は8.2待ち)を要件番号ベースで再整理 — 5/5要件Full(registrar自体のファイルI/O契約としては完全実装、実CLIでの受信確認は別途8.2.4で追跡)。詳細は`PARITY-AUDIT.md`
  - [x] 8.1.7 `wait-detection` — 5.6の簡略化注記(2段階タイマ統合・ReDoS対策未実装)、および7.3で追加したWaitBrokerフォールバックを要件番号ベースで再整理 — 4/6要件Full、2件Partial(ReDoS対策・2段階タイマ簡略化)。詳細は`PARITY-AUDIT.md`
  - [x] 8.1.8 `quick-command-register` — 5.7の未実装注記(右クリックメニュー・ダイアログ本体)を要件番号ベースで再整理 — 0/5要件Full、2件Partial(ロジックは実装・テスト済みだがUI導線が無く到達不能)、3件Missing。詳細は`PARITY-AUDIT.md`
  - [x] 8.1.9 `command-import-export` — 5.8の未実装注記(Export/Importダイアログ・ファイル選択UI)を要件番号ベースで再整理 — 3/6要件Full(データ層のコア機能)、1件Partial、2件Missing(選択・重複検出ダイアログ)。詳細は`PARITY-AUDIT.md`
  - [x] 8.1.10 `auto-send-idle` — 5.9の未実装注記(UI手動作成ペイン非対応)を要件番号ベースで再整理 — 5/5要件Full。全capability中最も完全。詳細は`PARITY-AUDIT.md`
  - [x] 8.1.11 `approval-hub` — 5.10の未実装注記(パイプ切断解放トリガー・ペインアクティブ化解放トリガー)を要件番号ベースで再整理 — 1/5要件Full、2件Partial、**2件Unverified**(attention状態自体の発火未確認・approveサブコマンドの人間操作込み完全E2E未実施)。詳細は`PARITY-AUDIT.md`、`pending-real-machine-verification.md`に新規2件追加
  - [x] 8.1.12 `ps-module` — 7.2で`Amm.PowerShell.Tauri`(.psm1)として実装・実機で一部コマンド確認済み。未確認コマンド(`Connect-Amm`/`Disconnect-Amm`)の実機確認と、旧`.NET`版`Amm.PowerShell`(WinForms向け)との共存確認が残る — 6/8要件Full、2件Partial(意図的な7.4→5.1引き下げ含む)、Missing 0件。7つのcmdlet全て実機live検証済み(Connect/Disconnect含む)。詳細は`PARITY-AUDIT.md`
  - [x] 8.1.13 `mcp-server` — phase4(4.1-4.6)の未実装注記(Named Pipe ACL、パイプ切断によるセッション解放)、および7.1/7.3で見つかった実装ギャップ2件の修正を要件番号ベースで再整理 — 7/9要件Full、1件Partial(ACL未実装)、**1件Missing(新規発見)**: `McpCliRegistrar`相当(amm-mcp.exe自体をClaude Code/Codex等のMCPサーバーとして自動登録する機能)が丸ごと未実装、hook-cliの`HookCliRegistrar`とは別物で従来ノーマークだった。詳細は`PARITY-AUDIT.md`
  - [x] 8.1.14 `mcp-gateway` — 外部MCPサーバー設定・自動起動・クラッシュ再起動等がphase4でどこまでカバーされたか要件番号ベースで確認(未着手の可能性あり、要調査) — **8/8要件Missing。未着手と判明**(`GatewayManager`/`ManagedMcpProcess`等の痕跡なし、grepで確認)。phase4の対象は`mcp-server`自体のプロトコルのみで外部サーバー集約は範囲外だった。従来のtasks.mdどのフェーズでも言及されておらず、今回の監査で新規発見した最大のギャップ。詳細は`PARITY-AUDIT.md`
  - [x] 8.1.15 `tray-icon` — phase3(3.3)の実装内容を要件番号ベースで確認、目視確認待ち分は8.2へ — 1/4要件Full、2件Partial、1件Missing(クリックでのセッションジャンプ機能)。詳細は`PARITY-AUDIT.md`
  - [x] 8.1.16 `approval-hub`のポップアップ表示(3.4)含め、既知バグ(WebviewWindowBuilder動的ウィンドウがCDPで見えない件)の扱いを8.1内で明記 — `PARITY-AUDIT.md`のapproval-hub「非モーダル集約ポップアップUI」requirementにUnverifiedとして明記済み(コード側は実データ表示まで実装済み、実機目視のみ未了)
    - **追記 2026-07-21**: 「CDPで見えない」は実は表示バグではなく🔴CRITICALなアプリ全体デッドロックだったと判明・実機デバッガで根本原因特定・第2WebviewWindowをやめメインウィンドウ内DOMオーバーレイへ置き換えて解消。実機で実際のフック経由承認→許可/拒否の一連をE2E確認済み。詳細は`tasks/pending-real-machine-verification.md`3.4項目。
- [x] 8.2 実機(Windows)での最終的な統合検証(全capability・全ペイン管理操作・全ネイティブ統合機能)(サブ項目8.2.1〜8.2.5全件完了。Codex CLIのhook-cli確認のみユーザー自身のサインインが必要なため次回持ち越し)
  - [x] 8.2.1 `tasks/pending-real-machine-verification.md`を1件ずつ実施・結果を追記(2026-07-22時点で全項目`[x]`化完了。実施済みの内容は各エントリ参照)
  - [x] 8.2.2 `.NET 9 SDK`導入環境で旧`.NET`版`Amm.Mcp.exe`/`Amm.PowerShell.dll`(WinForms向け、無変更)の実バイナリ疎通確認(4.4/4.5/5.11で協定した既知の穴。新Rust/`.psm1`版は7.1/7.2で実機確認済み) — 2026-07-21〜22 `dotnet-install.ps1`をユーザーローカルへ向けて実行しUAC不要で.NET 9 SDK(9.0.316)を導入、`Amm.Mcp.exe`は`dotnet build`後`amm-mcp.exe list`で新Rust版`amm.exe`(4.1のACL適用済み)へ実接続成功を確認。`Amm.PowerShell.dll`はWindows標準PowerShell 5.1・ポータブル版7.4.6(.NET 8)ではロード不可(`System.Runtime 9.0.0.0`未解決)と判明、.NET 9ベースのPowerShell 7.5.4を別途ポータブル導入してロード成功、`Connect-Amm`→`Get-AmmSession`で実データ取得を確認。詳細は`tasks/pending-real-machine-verification.md`。
  - [x] 8.2.3 `cargo-tauri`導入環境で`tools/build-installer-tauri.cmd`(7.5の更新後)を実行し、生成されたNSIS/MSIインストーラの実インストール・アンインストール・ファイル関連付け・(旧WiX版との)共存を確認 — 2026-07-21〜22 `cargo install tauri-cli --version ^2`導入後に実行、実バグ2件を発見・修正: (1) スクリプトが`cargo tauri build --release`という存在しない引数を渡していた(cargo-tauriは常にreleaseビルドのため引数自体が無い)、(2) `tauri.conf.json`に`mainBinaryName`未設定のためWiXの"Path"コンポーネントがメイン実行ファイルとして誤って`amm-mcp.exe`を参照し、`resources/*`で別途バンドルされる`amm-mcp.exe`と同一インストール先を2コンポーネントが指すICE30エラー(コンポーネント参照カウント破壊)でMSIビルドが失敗していた。`mainBinaryName: "amm"`追加で解消。副次的に発見した軽微な問題(`resources/.gitkeep`が誤ってインストーラに同梱される)も`build-installer-tauri.cmd`側でビルド前後に退避/復元する形で修正。修正後、NSIS/MSIとも実際にUAC承認の上でインストール→`amm.exe`/`amm-mcp.exe`起動・MCP疎通確認→アンインストール(レジストリ・ファイル・ショートカットとも完全削除)まで実機で確認。旧WiX版との共存確認は未実施(このマシンには旧WiX版が現時点でインストールされていなかったため対象外、将来的な確認課題として残る)。詳細は`tasks/pending-real-machine-verification.md`。
  - [x] 8.2.4 hook-cliの実CLIツール(Claude Code/Codex/Copilot CLI)での受信確認(8.1.6から継続) — 2026-07-21 Claude Code(実バイナリ、v2.1.216)で確認: 実際のPermissionRequestフックが発火しamm側の承認オーバーレイに実データが表示されることを実証。オーバーレイでの承認操作がClaude Code本体の対話プロンプトを自動解決しない現象も発見したが、`amm-mcp.exe`の出力はClaude Code公式仕様(`hookSpecificOutput.decision.behavior`)と完全一致しており、原因は[anthropics/claude-code#19298](https://github.com/anthropics/claude-code/issues/19298)として報告済みのClaude Code CLI側の既知バグ(decision無視)と特定、amm側の対応事項なしと結論。**2026-07-22追記**: Copilot CLIでも同様に実バイナリで確認(前回セッションの「起動しない」症状は本セッションでは再現せず一過性だったと判明)。フック登録→実際の権限確認プロンプト発火→amm側オーバーレイへの実データ表示まで確認、かつ「オーバーレイの許可がCLI本体のプロンプトを自動解決しない」制約もClaude Codeと同様に再現し、amm固有ではなく複数CLI共通のパターンと判明。Codexはインストール済みだが初回起動でサインインを要求されるため、ユーザー自身の認証情報が必要でありこのセッションでは未確認(次回持ち越し)。詳細は`tasks/pending-real-machine-verification.md`5.5項目。
  - [x] 8.2.5 `amm-mcp.exe approve`サブコマンドの人間操作を伴う完全なE2E確認(7.1で未実施と記録した分) — 2026-07-21〜22 Claude Code・Copilot CLIの両方で、実プロセスが発火した本物のPermissionRequestフック→`amm-mcp.exe approve`実行→承認オーバーレイへの実データ表示→「許可」/「拒否」クリック→正しいJSON応答、という一連を実機の実インタラクティブセッション上でE2E確認済み(承認・拒否ボタンのクリック自体はCDP経由の自動操作で行っており、文字通り人間の手によるクリックではない点は留意。ただしDOMオーバーレイに対する操作でありネイティブダイアログ特有のジェスチャ制約は受けないため、機能的な検証としては十分と判断)。詳細は`tasks/pending-real-machine-verification.md`5.5項目。
- [x] 8.3 旧WinForms版との並行運用期間を経て、切替(cutover)の判断を行う(判断基準は8.1のギャップ一覧+8.2の実機結果が出そろってから利用者側で検討する。AIは判断材料の整理までを担当し、切替可否の意思決定はしない) — 2026-07-26 ユーザーがTauri版への正式切替を決定。`src/apps/Amm.Tauri/`→`src/apps/Amm/`、旧`.NET`版`src/apps/Amm/`→`src/apps/Amm.net/`(同様に`Amm.Mcp`→`Amm.net/Amm.Mcp`、`Amm.PowerShell.Tauri`→`Amm.PowerShell`、旧`Amm.PowerShell`→`Amm.PowerShell.net`)というリネームで、Tauri(Rust)版を正式な主体実装、旧.NET版をレガシー(当面参照・保守のみ、廃止時期未定)として位置付け直した(`impl: save pre-rename state before Amm.Tauri cutover`(`21ab95b`)〜`impl: complete Amm.PowerShell.Tauri -> Amm.PowerShell module rename`(`7cc4dfc`)、2026-07-25〜26)。`README.md`/`docs/build.md`/`docs/design/architecture.md`/`CLAUDE.md`/`HANDOVER.md`を新パス・cutover完了ステータスに合わせて更新済み。
- [x] 8.4 利用者向けの移行ガイド(MDI→ペイン管理の操作体系変更点)を作成する(8.1.1のpane-management仕様確定後に着手) — 2026-07-22 `docs/manual/user-guide/migration-to-tauri.md`を新規作成。MDI→単一ウィンドウ内ペイン管理の概念差分、旧メニュー→新ツールバーの対応表、承認ハブ・`.amm`自動起動確認等の挙動変更点、新版で追加された機能(CLI登録UI・MCPゲートウェイ)、現時点の既知の未実装項目を整理。`usage.md`冒頭からもリンク。
- [x] 8.5 `docs/build.md` / `docs/design/architecture.md` / `CLAUDE.md` / `AGENTS.md` 等のドキュメントをTauriベースの構成に合わせて更新する(8.3のcutover判断が出るまでは「並行運用中」である旨を明記する形で更新し、旧WinForms版の記述は削除しない) — 2026-07-22 `docs/build.md`(Tauri版ビルド/テスト/publish/インストーラ手順とプロジェクト構成節を新設)・`docs/design/architecture.md`(Tauri版の成果物一覧・依存方向・主要サブシステム節を新設)・`CLAUDE.md`(リポジトリ構成ツリー・開発ワークフローにTauri版を併記、非Windows環境制約にTauri版の注記追加)を更新。いずれも既存の旧WinForms版記述は削除せず「並行運用中」の注記を追加する形で対応。`AGENTS.md`はプロジェクト概要をREADME.mdに委譲する構成のため本体の更新は不要と判断、代わりにREADME.md(AGENTS.mdが参照する実質的な参照先)も同様の観点で更新。
- [x] 8.6 `.NET`版ソースコードとの直接diffによるパリティ再監査(`PARITY-AUDIT-2026-07-20-source-diff.md`)で発見した修正タスク。8.1がspec文面ベースだったのに対し、本監査は.NET実ソースとTauri実ソースを1行単位で比較し、8.1が見落とした11件のCritical/High級ギャップおよびUI/UX改悪14件を新規発見した。仕様が既に要求している項目(A)と、仕様自体が沈黙していたため本セッションで`specs/{pane-management,profile-schema,wait-detection,hook-cli,input-history,git-integration}/spec.md`へ追記・明確化した項目(B)に分けて記載する。8.6.1〜8.6.16は元セッション(クラウド)でコード実装済み(下記参照)、**2026-07-21 実機Windowsセッションで8.6.1〜8.6.16全16項目の実機動作確認が完了**。うち8.6.1(`.amm`未承認自動起動確認、セキュリティレビュー🔴案件と同一)・8.6.11(「コマンド▶」メニュー)・8.6.15(`history.json`永続化)の3項目で、実装当初は気づかれていなかった新規の重大バグ計4件(未承認自動起動の確認ダイアログが実質到達不能だった2件・「コマンド▶」ボタンが一度も動作していなかった1件・旧`.NET`版との`history.json`パス衝突によるデータ消失1件)を発見し全て修正・実機E2E確認済み。残る13項目は実装通り動作することを確認(バグなし)。詳細は`tasks/pending-real-machine-verification.md`の8.6.1〜8.6.16各エントリ参照。
  - [x] 8.6.1 (A・既存spec違反) `pane-management`の「外部.ammファイルの自動起動確認」(spec.md:112-114)を実装する — セキュリティレビュー🔴案件と同一。`.amm`ダブルクリック起動時に`mcpServers`自動起動前の確認ダイアログが無く、確認なしの任意コード実行が成立する。実装: `profile.rs`(`is_path_trusted`/`mark_path_trusted`、`%LOCALAPPDATA%\amm\trusted-profiles.json`)、`gateway.rs`(`auto_start_entries`)、`lib.rs`の`.setup()`(未承認の明示パスなら`amm-untrusted-autostart-confirm`を発行して自動起動を保留、`confirm_untrusted_autostart`コマンドで承認後に起動+信頼登録)、`app.js`(確認ダイアログ)
  - [x] 8.6.2 (A・既存spec違反) `pane-management`の「終了時の確認ガード」中「未保存プロファイル変更の確認」(spec.md:108-110)を実装する — 現状プロファイル編集はメモリ上で可能なのに終了時の確認が一切ない。実装: `lib.rs`の`has_unsaved_profile_changes`(メモリ上とディスク上のProfilesFileを比較)、`app.js`の`onCloseRequested`に3択prompt(はい=保存して終了/空=保存せず終了/キャンセル=中止)を追加
  - [x] 8.6.3 (A・既存spec違反) `pane-management`の「記憶した配置の復元」中「未起動分は自動起動数設定に従って追加起動する」(spec.md:54)を実装する — `autoStartCount`はUIで設定・永続化されるが読み取って起動する経路が皆無。実装: `app.js`の`topUpAutoStartPanes`(起動時IIFEと`btn-restore`の両方から呼び出し、各プロファイルの生存ペイン数がautoStartCountに満たない分を`launchProfilePane`で追加起動)
  - [x] 8.6.4 (A・既存spec違反) `wait-detection`の「装飾のみの行を除外し直近50行を保持」中「両端の空白とフレーム文字を交互に剥がしてから正規表現に掛ける」(spec.md:34)を実装する — 現状は非交互・3文字限定の単発trimのみで、box形式プロンプトの即時マッチが劣化する。実装: `wait_detect.rs`の`trim_surrounding_frame`(全角枠線文字U+2500-257F対応、固定点まで繰り返しtrim)
  - [x] 8.6.5 (A・既存spec違反) `ps-module`の`Open-AmmWindow`/`Wait-AmmIdle`の終了エラー要件(spec.md:40,50-52,92,100)を実装する — 現状`Write-Error`(非終端)に格下げされており、パイプライン停止を前提にした自動化スクリプトが壊れる。同ファイル内`Connect-Amm`は正しく終了エラーを送出している。実装: `Amm.PowerShell.Tauri.psm1`の両cmdletで接続エラーと検証エラーを分離し、後者を`$PSCmdlet.ThrowTerminatingError`に変更(PowerShell実機での動作確認は未実施、実機`pwsh`/`powershell.exe`が無いこの環境では構文の目視確認のみ)
  - [x] 8.6.6 (A・既存spec/セキュリティレビュー) `mcp-server`のNamed Pipe ACL要件、`approval-hub`の非モーダル集約ポップアップUI要件(`show_approval_popup`のデッドロック)は8.1.11/8.1.13で既知。本監査で追加確認: 「承認ポップアップテスト」ボタン(`app.js`)が依然デッドロック経路に直結したまま残っており、恒久修正までは無効化を検討すること。実装: `app.js`の当該ボタンを`invoke('show_approval_popup')`直呼びから警告alertのみへ変更(デッドロック自体の根本原因は本タスクの対象外、`show_approval_popup`本体は未修正)
  - [x] 8.6.7 (B・spec追記済み→実装要) `profile-schema`の`theme`型不整合を修正する — Tauri側`profile.rs`の`theme: Option<String>`を`{ [key: string]: string }`相当へ変更し、1プロファイルの型エラーがファイル全体のロード失敗に波及しないようにする(spec.md「プロファイルファイルスキーマ」MODIFIED参照)。実装: `profile.rs`の`theme`を`Option<BTreeMap<String,String>>`+`deserialize_theme_lenient`(型不一致は例外を投げずNoneへ)
  - [x] 8.6.8 (B・spec追記済み→実装要) `profile-schema`の`autoChcp`/`ctrlCCopyOnSelection`/`closeOnExit`/`stats`の省略時デフォルトを`true`に修正する(spec.md「既定値でのロード」MODIFIED参照)。実装: `profile.rs`の該当4フィールドを`#[serde(default = "default_true")]`へ変更
  - [x] 8.6.9 (B・spec追記済み→実装要) `profile-schema`の新設要件「クローズ禁止設定によるペインクローズガード」を実装する — `closeProhibited=true`のペインクローズ時にShift押下以外をブロックする処理が実際のクローズ経路に一切無い。実装: `app.js`の`closePane`(✕ボタンのShiftキー状態を伝搬、プロファイルの`closeProhibited`を`list_profiles`で参照してブロック)。システムメニュー経由のクローズ等、✕ボタン以外の経路は未対応
  - [x] 8.6.10 (B・spec追記済み→実装要) `profile-schema`の新設要件「コマンド追加・編集ダイアログでの名前重複時の入力保持」を実装する — 現状は名前重複エラー時に入力済みドラフトが破棄され再入力が必要。実装: `app.js`の`openCommandTemplateDialog`に`checkNameConflict`引数を追加し、重複チェックを`closeModal`より前(ダイアログを閉じる前)に移動
  - [x] 8.6.11 (B・spec追記済み→実装要) `pane-management`の新設要件「コマンドメニューによるプロファイルのペイン起動」を実装する — 現状GUIの「+」ボタンは常に無設定シェルを起動するのみで、`profiles.amm`のコマンドをGUI単体からペインとして起動する手段が皆無(MCP/PowerShell経由でのみ可能)。実装: `mcp.rs`の`open_pane_for_gui`(MCP`pane/open`と同一の`open_pane`ロジックを再利用)、`lib.rs`の`launch_profile_pane`コマンド、`index.html`の「コマンド ▶」ボタン、`app.js`の`launchProfilePane`/クリックハンドラ
  - [x] 8.6.12 (B・spec追記済み→実装要) `pane-management`の新設要件「クイック切替バーの状態表示と操作メニュー」を実装する — 状態色/attentionアイコン欠落(既知)に加え、右クリックメニュー一式(クイック送信・チャット記録トグル等)が丸ごと無い。実装: `app.js`の`updateQuickSwitchBar`(状態色クラス付与・右クリックで`showPaneContextMenu`を再利用)、`style.css`に`.qsb-*`クラス追加
  - [x] 8.6.13 (B・spec追記済み→実装要) `wait-detection`の新設要件「強制状態通知は自動検知の巻き戻しから保護される」を実装する — 現状`force_state("busy")`後も常時稼働の200msポーリングスレッドが`last_output_at`を見ず、最短200msで`WaitingForInput`へ巻き戻るレースがある。実装: `wait_detect.rs`の`force_state`冒頭で`last_output_at`をリセット
  - [x] 8.6.14 (B・spec追記済み→実装要) `wait-detection`の新設要件「OSC 9 ターミナル通知によるattention検知」と`hook-cli`の対応するMODIFIED要件(Codexの`[tui]`具体値)を実装する — Codex CLIはhookベースのapproval-hub通知を持たないため、OSC9経路が唯一の通知手段だが現状フロントエンド(`app.js`)に一切実装が無い。実装: `hook_cli.rs`のCodex `[tui]`値を`.NET`原本通り(`notifications=["agent-turn-complete","approval-requested"]`/`notification_method="osc9"`)に修正(ユニットテストも追随)、`lib.rs`の`notify_pane_state`コマンド、`app.js`の`term.parser.registerOscHandler(9, ...)`
  - [x] 8.6.15 (B・spec追記済み→実装要) `input-history`の新設要件(永続化先の場所)を実装する — 現状`history.json`が実行ファイル隣接に保存され、`C:\Program Files\`等の標準インストール先では非管理者ユーザーが書き込めず履歴が永続化されない。実装: `input_history.rs`の`history_path`を`%LOCALAPPDATA%\amm\history.json`へ変更、`save`で親ディレクトリを`create_dir_all`
  - [x] 8.6.16 (B・spec追記済み→実装要、優先度低) `git-integration`の新設要件2件(status出力の非ASCIIファイル名表示・未プッシュ確認の既定アクション)を実装する。実装: `git_helper.rs`の`status_short`に`-c core.quotepath=false`追加、`app.js`の未プッシュ確認promptの既定値を`'push'`に変更(Enterでプッシュ)
  - [x] 8.6.17-a (未spec化、対応済み) 承認ポップアップの「閉じる」ボタン解放処理欠如/「ペインへ移動」ボタン欠落 — 2026-07-21 実機で対応・E2E確認済み。`app.js`の`approval-close`ハンドラが`resolve_approval(id, null)`を一切呼んでおらず、閉じても承認台帳に残り続けていた(旧`approval-popup.html`から引き継いだ既存バグ、この承認ハブ自体が到達不能だった間は無害だったが今回のオーバーレイ化で顕在化)。解放処理を追加し、新規「ペインへ移動」ボタン(`token`=pane_idで対象ペインを`bringToFront`/`setActivePane`)も実装。**実機テスト中に追加バグを発見・修正**: ✕ボタンが`.approval-count`div に隠れてクリック不能になっていた(DOM順序上後に来る要素が上に描画されるCSSスタッキングの問題)、`.approval-close`に`z-index:1`を追加して解消。実際の`amm-mcp.exe approve`で承認要求→✕クリック→`list_approvals`が空になることを確認、別の要求→「ペインへ移動」クリック→`activePane`が正しいpaneIdに変わり承認も解放されることを確認。
  - [x] 8.6.17-b-1 (未spec化、対応済み) チャット統計ダイアログの「プロファイル」列欠落 — 2026-07-21 実機で対応・E2E確認済み。`chat_stats.rs`の`ChatStatsRecord.profile`はバックエンド側に既にデータがあったがフロントエンドのテーブル描画(`openChatStatsDialog`)が使っていなかっただけ。「プロファイル」列をMDI列の前に追加。実機で`start_stats_tracking`→実コマンド送信→ダイアログ表示→新列に正しいプロファイル名が表示されることを確認。
  - [x] 8.6.17-b-2 (未spec化、対応済み) MCP CLI自己登録用UI(`McpRegistrationDialog`相当)皆無・ペインシステムメニュー統合による対象曖昧化 — 2026-07-21 両方とも実機で対応・E2E確認済み。
    - **MCP CLI自己登録UI**: `hook_register`/`mcp_register`/`hook_unregister`/`mcp_unregister`(実装・テスト済み)を呼ぶGUIボタンが皆無だった。ツールバーに「CLI への MCP 登録...」ボタンを新設、`openCliRegistrationDialog`で.NET版`McpRegistrationDialog`相当(4CLI×MCP/フックのチェックボックス・登録状態表示・差分適用)を実装。新規コマンド`get_mcp_exe_path`追加。**実機テスト中の事故**: `invoke`をモック化したつもりが`app.js`内で既に束縛済みの参照だったため効かず、実際の`~/.claude/settings.json`のフックが一時的に削除され、Codexに誤登録される事態が発生→直ちに再登録・登録解除して復旧(教訓: このアプリでの`invoke`モックは`window.__TAURI__.core.invoke`の差し替えでは効かない)。ダイアログの表示・読み取り・diff計算ロジック自体は正しいことを確認済み。
    - **ペインシステムメニューの対象曖昧化**: 各ペインの`.pane-title`に右クリックメニュー(名前変更/フォントサイズ/AMM設定、`runSystemMenuAction`に共通化)を新設、クリックしたペインを直接ターゲットにする(既存のOSシステムメニューは`resolveSendTarget()`の推測に依存したまま維持、追加の入口として機能)。実機で3ペイン作成→2番目のペインタイトルを右クリック→「名前変更」→2番目のペインだけが改名されることを確認(誤操作なし)。**実装時に発見**: `showCtxMenu`の裏で`document`の`contextmenu`リスナーが同一イベントで即座にメニューを閉じてしまうため`e.stopPropagation()`が必須(既存の`.pane-term`側の実装と同じパターン)。
