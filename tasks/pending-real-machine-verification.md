# pending-real-machine-verification — 実機での人間確認待ち一覧

> `migrate-to-tauri` 実装中、CDP経由の自動検証では確認できなかった(または対話的デスクトップセッションがない背景起動特有の制約で自動検証が信頼できなかった)項目をここに集約する。最後にまとめて実機で目視確認する。
> 確認できたら該当項目にチェックを入れ、確認結果(OK/NG・気づいた点)を追記すること。

- [x] **[add-pane-tiling tasks.md 7.1-7.8] ペインレイアウトのタイリングツリー方式全面置き換えの実機動作確認 — 2026-07-26調査の結果、対象機能が既に存在しないと判明(検証不要・チェック済み扱いとしてクローズ)**
  2026-07-23実装(このエントリの初版)からほどなくして、同一リポジトリ内の後続コミット`7520d68`(「pane grid rewrite」、`add-pane-tiling`のBSPツリー実装より後、このセッション開始時点で既に`main`へ反映済み)が、BSPタイリングツリー方式を「生存ペイン数から導出する決定的なrows×colsグリッド+並び順配列」というシンプルな方式へ全面置き換えていたことが2026-07-26のTask #41調査で判明した。`tilingRoot`/`dockNewPane`/`.tile-divider`/`insertPaneIntoTree`等、`add-pane-tiling`が新設したはずの関数・CSSクラスは現行コード(`src/apps/Amm/public/pane-layout.js`等)に一切存在しない。この設計変更(BSPツリー→シンプルグリッド)自体はUDR未記録だったため`UDR-amm-20260726T0950-c31`として事後記録し、`openspec/changes/add-pane-tiling/`は実装されなかった変更として`openspec/changes/archive/2026-07-26-add-pane-tiling-superseded/`へ「未実装のままsuperseded」の注記付きでアーカイブ、`openspec/specs/pane-management/spec.md`の該当Requirementを現行のグリッド+ドラッグ入れ替え+ズーム+並び順リセット方式の記述へ修正した。存在しない機能の実機検証はできないため、このエントリはクローズする。

- [x] **[tasks.md 6.1] インストーラのPATH自動追加(WiX `PathEnvironment`相当)の実インストール・アンインストール確認 — 2026-07-26 実施完了。重大バグを発見し修正、修正後は実機で完全往復を確認済み**
  MSI側は`wix/fragments/path-environment.wxs`(`<Environment Part="last" System="yes">`、MSIエンジン自体が追加/削除を処理、未検証のまま)、NSIS側は当初`nsis/hooks.nsh`(`NSIS_HOOK_POSTINSTALL`/`PREUNINSTALL`、`StrFunc.nsh`の`StrStr`/`StrRep`を使った手動PATH操作)で実装されていた。両方とも`cargo tauri build`で実際にエラーなくNSIS/MSIとも生成されることは2026-07-23時点で確認済み(過程で2件の実バグを発見・修正: (1) WiXフラグメント単体コンパイル時に`$(var.Win64)`が未定義だった問題、`main.wxs`とは別々に`candle.exe`でコンパイルされるため変数がファイル間で共有されないことが原因、x64固定でハードコードして解消。(2) NSIS側`${Using:StrFunc}`宣言がインストーラ用・アンインストーラ用で別々に必要と判明(`StrStr`/`StrRep`だけでなく`UnStrStr`/`UnStrRep`も明示宣言が必要)、追加して解消)。

  **2026-07-26、実際に実機でインストール→アンインストールを実施したところ、アンインストール時にシステムのMachine PATH環境変数を完全に空にする重大なバグを発見した**(実機PATHが30件・1234文字でNSIS標準文字列関数の暗黙の上限[`NSIS_MAX_STRLEN`≈1024文字]を超え、`ReadRegStr`が空文字列を返しそのまま上書きされていたことが原因。詳細な原因調査・修正内容は`tasks/retro-pending.md`2026-07-26エントリと`UDR-amm-20260726T1325-9d4`を参照)。NSIS側のPATH操作を`nsis/path-helper.ps1`(PowerShell、`Microsoft.Win32.Registry`経由、文字列長上限なし)への委譲方式へ全面的に書き換え、修正後に実機で以下を確認済み:
  - インストール: `C:\Program Files\amm`がMachine PATHへ正しく追加され、フレッシュなレジストリ読み取りで`amm.exe`がフルパス無しで解決できることを確認
  - GUI起動: インストール後の`amm.exe`が`Tauri Window`クラスの実ウィンドウを表示し、WebView2子プロセス6個が正常に起動することを確認(このバイナリ破損問題自体は`tasks/retro-pending.md`の別エントリ、amm.exe/amm-mcp.exeのハードリンク破損として記録)
  - アンインストール: `amm.exe`のみが削除され、Machine PATHの残り29件+amm自身の1件=30件が事前バックアップとバイト単位で完全一致することを確認(復元後の検証)

  **追記(2026-07-26、同日中に実施)**: MSI版(`wix/fragments/path-environment.wxs`)も実機で検証した。`msiexec /i`でインストール→GUI起動(WebView2子プロセス6個)・フレッシュレジストリでのPATH解決を確認→`msiexec /x`でアンインストール→Machine PATHがベースラインとバイト単位で完全一致(1234文字・30件・amm残留なし)することを確認。予想通りWiXのEnvironmentテーブルはWindows Installerエンジン自体が処理するため、NSIS版で見つかったNSIS_MAX_STRLEN起因のバグは発生しなかった。両インストーラ形式ともPATH自動追加の実機検証が完了。

- [x] **[PARITY-AUDIT.md approval-hub] 4種目の解放トリガー(パイプ切断による解放)を実装・実機E2E確認 — 2026-07-22 実装・単体テスト・実機で確認、OK**
  `ApprovalBroker::request()`が返すfutureを、同一接続上の非破壊的なpeek読み取り(`AsyncBufReadExt::fill_buf()`、EOFのときだけ空バッファを返しデータを消費しない)と`tokio::select!`でレースさせる形で実装(`mcp.rs`新設の`await_approval_or_disconnect`)。切断検知時は`request()`のfutureを破棄せず、`release_by_token`で明示的に解放してから最後まで`await`する設計とし、`ApprovalBroker::resolve()`側もエントリを即座に`pending`マップから削除するよう変更(`select!`でのfuture破棄によるリーク防止)。
  **単体テスト8件新規追加**(`approval.rs`に5件、これまでブローカー自体のテストが0件だった。`mcp.rs`に3件、`tokio::io::duplex`で疑似パイプペアを使い実際の切断シナリオを検証)。**テスト実行時にハマった点**: デフォルトの高並列度`cargo test`実行時にのみ1件のテストが不安定に失敗(45秒近くかかる)したが、`--test-threads=1`または`--test-threads=4`では即座に(1秒未満で)確実にpassすることを確認し、このセッション中に繰り返し発生していたOSスレッドレベルの資源競合(無関係なビルドプロセスが複数回外部から強制終了された事象と同種)が原因と判明、コード自体のバグではないと結論。
  **実機E2E確認(2パターン)**: (1) 実際の`amm-mcp.exe approve`サブプロセスを起動し`list_approvals`に実データが表示されることを確認→応答前にプロセスを強制killで切断→**約70msで承認オーバーレイから自動的に消える**(45秒タイムアウトを待たない)ことを確認。(2) 同じ手順で今度はkillせず`resolve_approval`で通常通り解決させ、正常な承認フローが引き続き機能することを確認(回帰なし)。

- [x] **[PARITY-AUDIT.md profile-schema] windowGeometryの「現在の配置を記憶」メニュー導線を実装・実機E2E確認 — 2026-07-22 実装・実機で確認、OK**
  バックエンドの`capture_window_geometry`コマンドは5.1から実装・単体テスト済みだったが呼び出すUI導線が無く到達不能だった(PARITY-AUDIT.mdで指摘済みの既知ギャップ)。ペインタイトルバーの右クリックメニューに「現在の配置を記憶」項目を追加し、`runSystemMenuAction('capture-geometry', pane)`から、クリックしたペインのプロファイルに属する全生存ペインの実座標(`x/y/w/h`)・`get_pane_working_dir`で取得した実作業ディレクトリを集めて呼び出す形で実装。
  **実機E2E確認(CDP経由)**: `CMD`プロファイルのペインを起動→座標を`(123,77,556,334)`へ変更→「現在の配置を記憶」実行→ステータス行に「「CMD」の配置を記憶しました(1件)」と表示されることを確認→`profiles.amm`に該当`windowGeometry`エントリ(x/y/w/h・name・workingDirectory)が正しく書き込まれることを確認→ペインをforce close→同じ`CMD`プロファイルを再度起動→**新規ペインが記憶した座標(x=123,y=77、w/hはCSSボックスモデルの丸めで1px程度の誤差)へ正確に復元される**ことを確認→作業ディレクトリも記憶した値(`C:\project\github\amm`)へ正しく復元されることを確認。`cargo test`(77+10件、Rustソース変更なしのためJS変更の影響確認のみ)も回帰なし。
  **意図的な簡略化**: このport にはペインごとの永続的な「最大化」フラグが無い(`maximizePaneToDesk`はその時点のデスク領域いっぱいにリサイズするだけで状態を保持しない)ため、記憶されるエントリは常に実座標のスナップショットであり`maximized: true`形式では保存しない。復元結果としては等価。

- [x] **[tasks.md 8.2.2] 旧`.NET`版`Amm.Mcp.exe`/`Amm.PowerShell.dll`と新Rust版`amm.exe`の疎通確認 — 2026-07-21 .NET 9 SDK/PowerShell 7.5をユーザーローカルへ導入の上、実機で確認、OK**
  従来UAC(管理者昇格)がブロックしていた`winget install`ではなく、公式`dotnet-install.ps1`スクリプト(`-InstallDir`をユーザーローカルへ指定)で.NET 9 SDK(9.0.316)をUAC不要でインストールできることを発見・実施。この方法で`src/apps/Amm.Mcp`(`dotnet build`)をビルドし、実行中のRust版`amm.exe`(4.1のACL適用済み名前付きパイプ)に対し`amm-mcp.exe list`を実行→実際のペイン一覧(JSON)が正しく返ることを確認(.NET側クライアント↔Rust側サーバーのワイヤプロトコル互換性、および制限DACL下でも同一ユーザーからの接続が壊れていないことを実証)。
  `Amm.PowerShell.dll`(`net9.0`)側は、Windows標準のPowerShell 5.1(.NET Framework)はもちろん、ポータブル版PowerShell 7.4.6(.NET 8ベース)でも`System.Runtime, Version=9.0.0.0`が解決できず`Import-Module`が失敗することが判明。.NET 9ベースのPowerShell 7.5.4を別途ポータブル導入(UAC不要、zip展開のみ)したところロード成功、`Connect-Amm`→`Get-AmmSession`で実際に稼働中のペイン一覧が返ることを確認。両バイナリとも新Rust版サーバーとの後方互換性が実証された。

- [x] **[tasks.md 8.2.3] `cargo-tauri`導入・NSIS/MSIインストーラの実ビルド・実インストール・アンインストール確認 — 2026-07-21〜22 実機で確認、実バグ3件を発見・修正、E2E確認完了**
  `cargo install tauri-cli --version ^2`(初回コンパイルに33分)でツールチェインを導入し、`tools\build-installer-tauri.cmd`を実行。
  **バグ1(発見・修正)**: スクリプトが`cargo tauri build --release`を呼んでいたが、`cargo tauri build`はそもそも常にreleaseモードでビルドする仕様(`-d/--debug`でデバッグへ切替、`--release`という引数自体が存在しない)であるため`error: unexpected argument '--release' found`で即座に失敗していた。`--release`を削除して修正。
  **バグ2(発見・修正、本命)**: バグ1修正後、NSISは成功したがMSIビルドが`light.exe`(WiXリンカ)で`ICE30: The target file 'amm-mcp.exe' is installed in '[ProgramFiles64Folder]\amm\' by two different components`エラーで失敗。原因は`tauri.conf.json`に`mainBinaryName`が未設定だったこと。このプロジェクトは`amm`(GUI)と`amm-mcp`(CLI companion)の2バイナリを持つが、`mainBinaryName`が無いとTauriのWiXバンドラが「PATHに登録すべきメイン実行ファイル」を誤って`amm-mcp.exe`と判定し、WiXの"Path"コンポーネント(本来`amm.exe`を指すべき)がintentionally-bundledな`resources/amm-mcp.exe`(`bundle.resources`の`"resources/*": "./"`glob経由)と同一インストール先を指す形になり、コンポーネント参照カウントの矛盾でMSIリンクが失敗していた。`tauri.conf.json`のトップレベルに`"mainBinaryName": "amm"`を追加して解消(WiXの"Path"コンポーネントが正しく`amm.exe`を参照するようになったことを生成後の`main.wxs`で確認)。
  **バグ3(発見・修正、軽微)**: `tauri.conf.json`の`bundle.resources`の`"resources/*": "./"`globが、gitトラッキング維持用の`resources/.gitkeep`(0バイト)も拾ってしまい、`C:\Program Files\amm\.gitkeep`として実際にエンドユーザーへ配布されていた。`build-installer-tauri.cmd`の`cargo tauri build`呼び出し前後で`.gitkeep`を削除→ビルド後に空ファイルとして復元する形で対処(git working treeもクリーンなまま維持)。
  **実機E2E確認(MSI)**: 修正後、実際に`msiexec /i amm_1.2.0_x64_en-US.msi`を実行(ユーザーにUAC承認を依頼)→`C:\Program Files\amm\`へ`amm.exe`/`amm-mcp.exe`/`Amm.PowerShell.Tauri`/`profiles.amm`/`usage.md`/アンインストール用ショートカットが正しく配置される(ICE30の原因だった重複なし、`.gitkeep`も含まれない)ことをファイル一覧で確認。Add/Remove Programsへの登録も確認(`DisplayName=amm`, `DisplayVersion=1.2.0`)。アンインストールは`msiexec /x{ProductCode} /quiet`だと**Error 1730「管理者として実行する必要があります」**で失敗すると判明(`/quiet`だとUAC同意ダイアログを表示できないため、通常の`/i`が受けるOS標準の自動昇格プロンプトの恩恵を受けられない)。`/quiet`無し(対話モード)で再実行しユーザーにUAC承認+ウィザード操作を依頼したところ成功、`C:\Program Files\amm\`・レジストリのAdd/Remove Programsエントリとも完全に削除されたことを確認。
  **実機E2E確認(NSIS)**: 同様に`amm_1.2.0_x64-setup.exe`を実行(UAC承認)→インストール成功、ファイル一覧はMSIと同一構成(`uninstall.exe`が追加)。1回目のアンインストーラ起動はUACがユーザーにより意図せずキャンセルされたが、再試行で成功。アンインストール後、ファイル・レジストリとも完全に削除されたことを確認。
  **旧WiX版との共存確認**: 未実施(このマシンに旧WiX版が現時点でインストールされていなかったため対象外。旧版の残留ログファイル(`%LOCALAPPDATA%\amm\log\app.log`、v1.1.0.0、2026-06-28/30の日付)は見つかったが、対応するMSI登録は既に存在しておらずインストール状態としては残っていなかった)。両インストーラのUpgradeCode(旧WiX: `8E3C7A9B-...`、新Tauri: `1feb6c68-...`)は異なるため、理論上は共存可能なはずだが実インストール環境での相互非干渉の実証は次回セッションへ持ち越し。
  **副次的に確認した重要事項(MCPパイプ接続の起動時レイテンシ)**: `target/release/amm.exe`をインストーラ経由でなく直接実行した場合、MCP名前付きパイプが確立するまで実測6〜12秒程度かかることを確認(デバッグビルドは終始1〜2秒未満で即座に確立していたのと対照的)。`Test-Path`は名前付きパイプに対して信頼できない(サーバーが存在しなくても`True`を返す例を確認)ため、`amm-mcp.exe list`の実接続成功を正としてground truthとした。一時的にコード側へファイル書き込み診断(`std::fs::write`)を追加して原因調査したが、診断ログ自体が(`C:\`直下は書き込み権限なし、`%TEMP%`でも環境要因で再現せず)信頼できる形で書けなかったため、コード側の分岐特定には至らなかった(診断コードは全て削除済み、`git diff`でmcp.rsに変更が残っていないことを確認済み)。`C:\Program Files\amm\`にインストールされた実体では、この遅延がさらに長く(数分単位)なるケースを複数回観測しており、Windows Defenderによる未署名実行ファイルへのリアルタイムスキャン、または実機セッションが人間と共有されている影響(該当ウィンドウをユーザーが閉じた可能性を本人から示唆された)のいずれか、または両方が原因と推測しているが、確定的な原因特定はできていない。**amm自身のRustコード(ACL実装・パイプ作成ロジック)に構造的な欠陥がある証拠は見つからなかった**(`cargo test`のACL関連テストは全てpass、`target/release/amm.exe`を直接実行した場合は複数回とも待てば必ず接続に成功した)。実運用上の影響は「起動直後の数秒〜数分、外部CLIツールからのMCP接続が失敗しうる」程度で、GUI自体の動作(ウィンドウ表示・ペイン操作等)には影響しない。コード署名や本番配布時のAV事前許可リスト登録で改善が見込まれる。次回セッションでの持ち越し課題として記録。

- [x] **[tasks.md 4.1] Named Pipe ACL(現在ユーザーのみアクセス許可)の実機実装・確認 — 2026-07-21 実装・単体テスト・実機接続確認完了**
  `.NET`版`McpPipeServer.cs`の`PipeSecurity`(現在のWindowsユーザーのみFullControl)相当をRust側で実装。SDDL文字列(`current_user_sid_string()`でトークンから解決したSIDを埋め込んだ`D:(A;;GA;;;{sid})`)を`ConvertStringSecurityDescriptorToSecurityDescriptorW`でセキュリティ記述子へ変換し、`ServerOptions::create_with_security_attributes_raw`(unsafe)でパイプ生成時に適用。属性構築(SID解決・SDDL変換)のいずれかが失敗した場合は例外を投げず、ログを出して既定の(無制限)パイプセキュリティにフォールバックする設計(`spawn_server`内`create_pipe!`マクロ)。`Cargo.toml`の`windows`クレートに`Win32_Security_Authorization`フィーチャを追加。
  **実装中に発生したハマりどころ**: `GetSecurityInfo`で生成済みパイプの実DACLを読み戻すテストが`E_INVALIDARG (0x80070057)`で失敗。原因はオブジェクト種別に`SE_FILE_OBJECT`(ファイルシステムオブジェクト用)を指定していたことで、名前付きパイプのハンドルは`SE_KERNEL_OBJECT`を指定する必要があった。修正後はテスト成功。
  **単体テスト2件を新規追加(`mcp.rs`内`acl_tests`モジュール)**: (1) `current_user_sid_string_looks_like_a_sid` — 現在プロセストークンから解決したSIDが`"S-1-5-21-"`(通常アカウントSIDの標準プレフィックス)で始まることを確認。(2) `pipe_dacl_grants_exactly_one_ace_to_the_current_user` — 実際にテスト用パイプを本実装の関数で生成し、`GetSecurityInfo`→`GetAclInformation`→`GetAce`で実DACLを読み戻し、ACEが現在ユーザーに対して1件のみ存在することを確認。両方ともpass。`cargo test`全体(77+10件)も回帰なし。
  **実機E2E確認**: `cargo build`済みの`amm.exe`(debugビルド)を実際に起動し、同一ユーザーの`amm-mcp.exe list`が引き続き正常応答(`[]`)することを確認(ACL制限後も同一ユーザーからの正規接続が壊れていないことを実証)。異なるユーザーアカウントでの接続拒否確認は、このマシンに複数ユーザーアカウントが無いため未実施(将来的な確認課題として残る、ロジック自体はSDDLで単一SIDのみ許可する構成のため理論上は拒否されるはず)。

- [x] **[tasks.md 8.6.17-b-2] MCP CLI自己登録UI・ペインシステムメニュー対象曖昧化の実機動作確認 — 2026-07-21 両方とも実装・E2E確認完了、実機テスト中に実環境への意図しない変更事故が発生**
  **MCP CLI自己登録UI**: 新設した「CLI への MCP 登録...」ダイアログを実機で開き、実際の登録状態(このマシンには以前のセッションでClaude Codeのフックが登録済み・MCPサーバーが`C:\Program Files\amm\amm-mcp.exe`という別パスで登録済み)が正しく反映されること(「登録済」「登録済 (パスが異なるため適用時に更新)」「未登録」の3状態)を確認。
  **⚠️ 実機テスト中の事故**: 「適用」ボタンの動作を安全に検証するため`invoke`をモック化する意図で`window.__TAURI__.core.invoke`を差し替えたが、`app.js`は起動時に`const { invoke } = window.__TAURI__.core`でローカル参照を既に確保しているためモックが効かず、**実際に`mcp_register('claudeCode')`・`mcp_register('codex')`・`hook_unregister('claudeCode')`が実行されてしまった**。結果、ユーザーの実際の`~/.claude/settings.json`からフック設定(Notification/PermissionRequest/Stop)が全て消失し、Codexの`config.toml`に意図しないamm登録が追加された。**直ちに`hook_register('claudeCode')`で再登録・`mcp_unregister('codex')`で登録解除して復旧**、`~/.claude/settings.json`に3フックが正しく戻っていること・Codexの`config.toml`にamm関連の追記が無いことを確認。Claude CodeのMCPサーバー登録パス(Program Files→現在のdebugビルドパスへの更新)は意図した機能通りの動作のためそのまま維持。**教訓**: このアプリのフロントエンドで`invoke`をテストダブルに差し替えるには、`window.__TAURI__.core.invoke`ではなく該当関数を直接使っている箇所ごとに介入する必要がある(単純なグローバル差し替えでは効かない)。実際にグローバル状態(このマシンのCLIツール設定等)を書き換えるボタンをテストする際は、事前にモックが本当に効いているか極小さな変更で確認してから本テストに進むこと。
  **ペインシステムメニューの対象曖昧化**: 3ペイン作成→2番目のペインを前面化→そのペインのタイトルバーを右クリック→「名前変更」→**2番目のペインだけ**が改名され他のペインは無変更であることを確認(誤操作なしを実証)。実装中に発見した`stopPropagation()`未設定によるメニュー即時クローズのバグも修正済み。

- [x] **[tasks.md 8.6.17-a] 承認オーバーレイの「閉じる」解放処理・「ペインへ移動」ボタンの実機動作確認 — 2026-07-21 実機で追加バグ1件を発見・修正、E2E確認完了**
  `PARITY-AUDIT-2026-07-20-source-diff.md`指摘の2件(✕ボタンが`resolve_approval`を呼ばず解放されない・「ペインへ移動」ボタン皆無)を実装。実機での動作確認中に**3件目の未知のバグを新規発見**: ✕ボタン(`.approval-close`、`position:absolute`)がDOM順序上後に来る`.approval-count`(通常フローのdiv)に画面上覆われてクリック不能になっていた(Playwrightの`page.click`が「element intercepts pointer events」で自動検出)。`.approval-close`に`z-index:1`を追加して解消。実機で: 実際の`amm-mcp.exe approve`で承認要求発生→✕クリック→`list_approvals`が空になる(解放確認)、別の要求→「ペインへ移動」クリック→`activePane`が正しいpaneIdに切り替わり承認も解放されることを確認。`openspec/changes/migrate-to-tauri/specs/approval-hub/spec.md`に2シナリオ追加。

- [x] **[tasks.md 8.6.1] `.amm`未承認自動起動確認ダイアログの実機動作確認 — 2026-07-21 セキュリティレビュー🔴案件と同一、実機で重大バグ2件を発見・修正・E2E確認完了**
  実機テスト用に`mcpServers`に`autoStart:true`のエントリを持つ`tests/e2e-tauri/fixtures/untrusted-test.amm`を新規作成し、`amm.exe "<そのパス>"`(明示的CLI引数、`.amm`ダブルクリック起動相当)で起動して検証。
  **バグ1(発見・修正)**: 確認ダイアログが一度も表示されなかった。`.setup()`が`app.emit("amm-untrusted-autostart-confirm", ...)`する時点では、webviewがまだ`index.html`のナビゲーションすら完了しておらず`app.js`の`listen()`登録が間に合っていないため、イベントが毎回サイレントに失われていた(`gateway_server_infos`が`stopped`のまま・`trusted-profiles.json`が生成されない、で発覚)。セキュリティ的には「自動起動されない」という安全側の結果になっていたが、ユーザーが承認する手段自体が無い実質デッドロック状態だった。**修正**: push(emit)方式をやめ、`.setup()`側で`PendingUntrustedAutostart`状態に保存→フロントエンド起動時に`check_pending_untrusted_autostart`で一度だけpullする方式に変更(`.setup()`はTauriのIPCが使えるようになる前に完了するため、pull側がレースを起こしようがない)。
  **バグ2(発見・修正)**: バグ1修正後もダイアログが見えず、CDP経由で直接`confirm()`を呼ぶと`"dialog.confirm not allowed. Command not found"`で即座に失敗することが判明。原因はこの呼び出しがボタンクリック等の実ユーザー操作(トラステッドジェスチャ)を伴わない、アプリ起動時の自動実行コンテキストからの`window.confirm()`呼び出しであること(このアプリの他の`confirm()`呼び出しは全てクリックハンドラ内=ユーザージェスチャ有りで、これだけが例外的に自動発火だった)。Chromium/WebView2はユーザージェスチャの無いネイティブダイアログAPI呼び出しを信頼できず抑制し得ることを実機で確認。**修正**: `window.confirm()`をやめ、既存の`openModal`(DOMベースのモーダル、他のダイアログと共通の仕組み)に置き換え。
  **実機E2E確認**: 未承認ファイルで起動→モーダルにパス・自動起動コマンド一覧が正しく表示されることを確認→「許可」クリック→`gateway_server_infos`が`stopped`→`running`(toolCount:1)に変化・`trusted-profiles.json`に当該パスが追記されることを確認→再起動(同じ既に信頼済みのファイル)→モーダル非表示のままサーバーが自動起動することを確認→リセットして再度未承認状態から「拒否」→サーバーが`stopped`のまま・`trusted-profiles.json`が生成されないことを確認。許可/拒否/再起動時の信頼永続化、全パターン実データで確認済み。

- [x] **[tasks.md 8.6.2] 未保存プロファイル変更の終了確認ダイアログの実機動作確認 — 2026-07-21 本物のネイティブダイアログでE2E確認、OK**
  `update_profile_settings`(persist=false)でメモリ上のみのプロファイル変更を作成→`has_unsaved_profile_changes`が`true`を返すことを確認→Win32 `WM_SYSCOMMAND`/`SC_CLOSE`をamm本体のHWNDへ`PostMessage`(タイトルバー✕クリックと同一の実イベント経路)で実際のウィンドウクローズを発火。UI Automationで実際にネイティブダイアログ(WebView2の`confirm()`/`prompt()`、タイトル「tauri.localhost の内容」)を検出・操作: (1) 実行中セッションの確認`confirm()`→「OK」クリック、(2) このリポジトリ自身の未プッシュコミットに対するgit guardの`prompt()`(意図しない実pushを避けるため`ValuePattern.SetValue`で入力欄をクリア/操作、実際にリモートへpushされていないことを`git log origin/...`で確認済み)、(3) 本題の未保存プロファイル変更`prompt()`→入力欄に「はい」をセットして「OK」→**実際にプロセスが終了し、`profiles.amm`に変更(テスト用マーカー文字列)が正しく書き込まれる**ことを確認。8.6.1のバグ2(ユーザージェスチャ無しのconfirm/promptが抑制される)とは異なり、こちらは実際のOSウィンドウクローズ操作(タイトルバー✕相当)がトリガーなので、モンキーパッチ無しの本物のネイティブダイアログが正しく機能することを実証。

- [x] **[tasks.md 8.6.16] git未プッシュ確認の既定アクション(Enterでpush)の実機動作確認 — 2026-07-21 8.6.2の検証中に実データで確認、OK**
  8.6.2の実機E2Eテスト中、このリポジトリ自身の未プッシュコミットに対して実際にgit guardの`prompt()`が発火し、入力欄に既定値`"push"`が正しくプリセットされている状態を実機ネイティブダイアログで目視確認済み(UI Automationの`ValuePattern`経由でも既定値を確認)。Enterでそのままプッシュされる設計が実装通り機能していることを実証(実際にpushはしていない、誤動作防止のため入力欄をクリアしてスキップした)。

- [x] **[tasks.md 8.6.7/8.6.8] `theme`型不整合の寛容な処理・4bool既定値trueの実機動作確認 — 2026-07-21 実データで確認、OK(バグ無し)**
  `tests/e2e-tauri/fixtures/defaults-test.amm`(`theme`をオブジェクトでなく文字列で指定、`autoChcp`/`ctrlCCopyOnSelection`/`stats`/`closeOnExit`を省略)を新規作成し明示パスで起動。`list_profiles`で実データ確認: (1) `theme`型不一致でもファイル全体のロードが失敗せず、当該プロファイルの`theme`は`null`として正常にロードされた。(2) 省略した4項目は全て`true`で復元された。8.6.7/8.6.8とも実装通り、新規バグなし。

- [x] **[tasks.md 8.6.3] `autoStartCount`復元時の追加起動の実機動作確認 — 2026-07-21 実機で確認、OK(バグ無し)**
  `autoStartCount: 3`の新規プロファイルを含む`tests/e2e-tauri/fixtures/autostart-count-test.amm`で起動→`panes`配列を実際に確認し、`profileName === 'AutoStartCountTest'`のペインが正確に3件自動起動されることを確認。

- [x] **[tasks.md 8.6.4] `wait-detection`のフレーム除去(交互trim)の動作確認 — 2026-07-21 Rustユニットテスト4件を新規追加して確認、OK(バグ無し)**
  `trim_surrounding_frame`はPTY出力を処理する純粋関数のため、実機PTY経由よりRustユニットテストの方が同じコードパスを直接・高信頼に検証できると判断し、`wait_detect.rs`にテストモジュールを新規追加(このファイルにテストが1件も無かった)。`"│ > │"`(全角枠線+空白)が単発trimでは`" > "`(先頭空白が残り`^>`にマッチしない)になってしまうところ、交互trimの実装により正しく`">"`まで剥がれ、`WaitPatternDetector::matches_any`が即座にマッチすることを確認。ASCII`|`混在・装飾なしプレーンテキストの回帰も追加。

- [x] **[tasks.md 8.6.10] コマンド追加ダイアログの名前重複時ドラフト保持の実機動作確認 — 2026-07-21 実機で確認、OK(バグ無し)**
  「コマンドを管理...」→「追加...」→名前に既存プロファイル名(`CMD`)・実行ファイルに識別用マーカー文字列を入力→OK→重複アラート表示後もダイアログが閉じず、入力した名前・実行ファイルとも消えずに残ることを実機で確認。

- [x] **[tasks.md 8.6.13] `force_state`のレース保護の動作確認 — 2026-07-21 Rustユニットテスト追加で確認、OK(バグ無し)**
  `wait_detect.rs`に`force_state_resets_silence_clock_to_protect_against_rewind`を追加。`last_output_at`を意図的に4.5秒過去へ巻き戻した状態で`force_state("busy")`を呼び、直後の`check_silence()`が(修正前なら即座にWaitingForInputへ巻き戻るところ)`false`を返し状態がRunningのまま保たれることを確認。

- [x] **[tasks.md 8.6.14] OSC9 attention検知(Codex向け)の実機動作確認 — 2026-07-21 実機で確認、OK(バグ無し、仕様通りの挙動を再確認)**
  非アクティブペインの`term.write()`で実際に`\x1b]9;approval needed\x07`(OSC9)を注入→`notify_pane_state`経由で`attention: true`に変化することを確認。続けて`\x1b]9;Turn complete\x07`(承認/permission語彙を含まない)を注入→`state`は`idle`扱いだが`attention`は`true`のまま変化しないことを確認。これは`wait_detect.rs`の`force_state("idle")`分岐が`has_attention`に一切触れない設計(`notify_mapper.rs`の他CLI向けマッピングとも整合、"idle"はattentionを解除せずアクティブ化/"busy"のみが解除するのが仕様通り)と一致しており、バグではなく意図した挙動であることを確認。

- [x] **[tasks.md 8.6.5] `Open-AmmWindow`/`Wait-AmmIdle`の終了エラー契約の実機動作確認 — 2026-07-21 実機の`powershell.exe`(5.1)で確認、OK(バグ無し)**
  実機`amm.exe`を起動し、`Amm.PowerShell.Tauri.psm1`を`Import-Module`。`Wait-AmmIdle -Nickname "存在しないニックネーム"`を`try/catch`で実行→実際に`ThrowTerminatingError`(ErrorId=`WaitAmmIdleNicknameNotFound`)がスローされ`catch`ブロックで捕捉されることを確認(仕様`ps-module`の「見つからない場合は終了エラーとしなければならない (MUST)」シナリオと一致)。
  **検証中の分析(バグではなく仕様確認)**: `Open-AmmWindow -Command <存在しない実行ファイルパス>`(spawn失敗)を試したところ非終了`Write-Error`になったが、これは仕様上正しい: `Send-AmmPipeRequest`はJSON-RPCレベルのエラー応答(spawn失敗等のアプリケーションエラー)を例外としてスローし、`Open-AmmWindow`の外側catch(接続失敗用、意図的に非終了)がそれを捕捉する設計になっている(`.NET`原本の`AmmPipeClient.SendRequest`と同じ契約)。仕様(`ps-module`ドキュメントの「session_idが空、またはresult自体が得られない場合」)が終了エラー化を要求しているのは「RPC自体は成功したがsession_idが欠落している」という別のケースであり、spawn失敗のような明示的RPCエラー応答は元から対象外。`ThrowTerminatingError`自体の実装(`$PSCmdlet.ThrowTerminatingError`呼び出し)は`Wait-AmmIdle`側で実機確認済みの同一パターンのため、`Open-AmmWindow`側も構造的に正しいと判断。

- [x] **[phase 2 / tasks.md 2.7] 終了確認ガードの「はい」選択時に実際に終了するか — 2026-07-20 実機で「OK/キャンセルどちらを押しても終了しない」バグを発見・修正**
  実行中セッションがある状態で閉じようとした際に出る確認ダイアログについて、「キャンセル」で終了が阻止されることはCDP経由で実機確認済みだったが、今回ユーザーが実機で「はい」を選んでも「キャンセル」を選んでも一切先に進まない(ウィンドウが閉じない)ことを発見。原因は`capabilities/default.json`に`core:window:allow-close`はあるが`core:window:allow-destroy`が無く、Tauriの`onCloseRequested`ラッパーが確認完了後に内部で呼ぶ`.destroy()`が権限拒否で黙って失敗していたこと(コンソールにエラーは出るがユーザーには見えない)。`core:window:allow-destroy`を追加して解消。CDP経由でconfirm/promptをすべて通した上で実際にプロセスが終了することを確認済み(修正前は同じ手順でプロセスが残り続けることも確認して原因を特定した)。

- [x] **[phase 3 / tasks.md 3.2] ファイルドラッグ&ドロップの実OS操作での動作確認 — 2026-07-20 実機で2つのバグを発見・両方修正、最終確認OK**
  **バグ1(修正済み)**: 人間が実機で「ペインへドロップしても必ず共通入力欄(下部エディットボックス)へ挿入される」と報告。`.NET`版`TerminalChildForm.cs`の`HandleDroppedFiles`はドロップした**そのペインのシェル入力バッファへ直接**`SendText`しており、この移植先自身の`migrate-to-tauri`仕様(`ファイルドラッグ&ドロップ`要件、「入力欄およびペイン領域へのファイルドロップ」)にも同様の記載があるにもかかわらず、Tauri版は常に共通入力欄へ挿入する実装になっていた。`install_drag_drop_handler`(`lib.rs`)がドロップ位置(物理px)をCSS pxに変換して`amm-files-dropped`イベントに含め、フロントエンド(`app.js`)が`document.elementFromPoint`でペイン要素を特定→そのペインの`pty_write`へ直接書き込む(パスモードは改行なし、内容モードは`\r`付きで実行)よう修正(コミット`398e373`)。
  **バグ2(修正済み)**: `.md`ファイルをドロップしても内容/パス選択の確認ダイアログが一度も表示されなかった。診断ログで、ブラウザ標準`confirm()`がこの環境ではOS-drag-drop起因のイベントハンドラから呼ぶとブロックせず**Promiseオブジェクトを返す**(常にtruthy)ことが判明(`tests/e2e-tauri/README.md`記載の制約がCDP限定ではなかった)。`tauri-plugin-dialog`のネイティブダイアログへ置き換え(コミット`8631ae9`)、さらにユーザーからのフィードバックで「内容/パス」の単純な2択(OK/Cancel)ではなく`.NET`版`TerminalChildForm.cs`の`AskDropAction`(Win32 TaskDialogの3択ボタン)相当の「内容送信/絶対パス送信/キャンセル」3択に作り直し(`MessageDialogButtons::YesNoCancelCustom`、コミット`0dd3d51`)。**実機で3択ダイアログの表示・各選択後の動作を確認、OK。**

- [x] **[phase 3 / tasks.md 3.3] トレイアイコンの目視確認 — 2026-07-20 実機で確認、OK(集中モードに注意)**
  `Shell_TrayWnd`の存在・可視状態の確認に加え、右クリックコンテキストメニューの表示・各項目の動作を人間が実機で確認済み(前セッション)。今回、バルーン通知(`show_attention_notification`)についても実機確認: ペインをattention状態にしただけでは通知が出ず、原因はWindows側の集中モード(Focus Assist)が通知を抑制していたためと判明。集中モードをオフにし、ペイン状態を一度別状態にリセットしてから再度attentionへ遷移させたところ(同一状態への再遷移は`WaitPatternDetector::transition`が変化なしと判定し通知を再発火しないため、一度リセットが必要)、正しくバルーン通知が表示された。アプリ側の不具合ではなくWindows設定起因。なお、このセッション自身の`System.Drawing.CopyFromScreen`によるスクリーンショットではタスクバー領域が一切キャプチャされない(DWM合成要素がGDIキャプチャに映らない)制約は引き続きあるため、目視確認は人間側で実施した。

- [x] **[phase 3 / tasks.md 3.4] `show_approval_popup`ハングの根本原因特定・オーバーレイdiv化での修正・実機確認 — 2026-07-21 完全解消**
  前回セッションまでの調査(下記の旧エントリ参照)で「WRY/WebView2の第2ウィンドウ生成処理内部が原因、実機デバッガが必要」まで絞り込まれていた。今回、実機に`winget`で Windows SDK の「Debugging Tools for Windows」機能のみをインストール(`OptionId.WindowsDesktopDebuggers`、UAC昇格が2段階必要でUI Automation経由のクリック+ユーザーによるUAC承認2回で対応)、`cdb.exe`を入手。
  **再現手順**: `amm.exe`を`WEBVIEW2_ADDITIONAL_BROWSER_ARGUMENTS=--remote-debugging-port=9333`で起動→Playwright CDPで`invoke('show_approval_popup')`をfire-and-forgetで発火(await せず切断、`tests/e2e-tauri/probe-trigger-hang2.js`参照、削除済みなので再実行する場合は`README.md`のパターンで作成)→2秒後に`list_approvals`を5秒タイムアウト付きで叩くとタイムアウト(ハング確認)。
  **原因特定**: `cdb.exe -p <pid> -c "~*kb 30;!runaway;q"`で全12スレッドのコールスタックを採取。メインスレッド("main")が以下の呼び出し経路で完全に停止していた(内側から): `webview2_com::wait_with_pump`(`GetMessageA`ループで第2WebviewWindowの`CoreWebView2Controller`生成完了を同期待機)← `wry::webview2::InnerWebView::create_controller` ← `WebviewWindowBuilder::build` ← `amm_lib::show_approval_popup` ← `tauri::webview::Webview::on_message` ← ... ← `EmbeddedBrowserWebView!...ReceivedCdpMessage`/`FireWebResourceRequestedEvent`。
  **結論**: TauriのIPC配送はメインwebview自身のCDPネットワーク傍受(`WebResourceRequested`/`Fetch.requestPaused`)経由でメインUIスレッド上に届く。`show_approval_popup`はこのコールバックの**内側**で同期実行され、そこから第2WebviewWindowを`build()`すると、その完了待ち(`wait_with_pump`)がメインスレッド上でネストしたWin32メッセージループを回す。しかし第2コントローラの非同期生成はWebView2ブラウザプロセスとのラウンドトリップが必要で、ブラウザプロセス側は「メインスレッドが最初のCDPリクエスト処理(=このコマンド自身)を終えるまで」他の要求を処理できない。メインスレッドはブラウザプロセスの応答待ち、ブラウザプロセスはメインスレッドの応答待ち、という循環待機で**タイムアウトなく永久にデッドロック**する。`!runaway`で全スレッドCPU時間0.2秒未満(1分以上ハングした状態で)であることも確認し、スピンではなく純粋な待機であることを裏付けた。
  この構造上、過去に試した対策(decorations除去・set_size nudge・`app.run_on_main_thread`でのラップ)がどれも効かなかった理由も説明できる: いずれも「メインwebviewの IPC コールバックの内側から、同じメインスレッド上で第2の WebviewWindow/CoreWebView2Controller を生成する」という条件自体は変わらないため。
  **対応**: `show_approval_popup`コマンド・第2`WebviewWindow`・`approval-popup.html`を全て削除。代わりに`index.html`にメインウィンドウ内の固定位置div(`#approval-overlay`、右上・オレンジ・280px)を追加し、`app.js`に`refreshApprovalOverlay`/`decideApprovalOverlay`(既存の`list_approvals`/`resolve_approval`/`release_approval_on_activate`をそのまま再利用、バックエンド側は無変更)を実装、`amm-approval-requested`イベントの購読を再度有効化。`style.css`に`.approval-overlay`系クラス追加。`capabilities/default.json`の`windows`配列から`approval-popup`を削除。手動テストボタン(「承認一覧を表示」に改称)も安全な`refreshApprovalOverlay()`呼び出しに戻した。
  **実機E2E確認(2026-07-21)**: `cargo build`/`cargo test`(70件、新規2件含め全pass)→実機起動→実際の`amm-mcp.exe approve --source test`(`AMM_NOTIFY_ID`に実ペインID指定、stdin JSON経由でtool_name/tool_input)を2回実行。(1)1回目: オーバーレイが実データ("Bash"・実際の引数JSON)で右上に表示されることをスクリーンショットで確認、「許可」クリック→オーバーレイが消え、`amm-mcp.exe`の標準出力に`{"hookSpecificOutput":{"decision":{"behavior":"allow"},...}}`(Claude CodeのPermissionRequestフック期待形式)が正しく出力されることを確認。クリック後も`list_approvals`が8msで応答(**ハングなし**)。(2)2回目: 「拒否」クリック→`{"decision":{"behavior":"deny","message":"Denied by the user via amm Approval Hub."}}`を確認。実際のフック発火→UI表示→人間の意思決定→CLIへの応答、という一連が実データで正常動作することをE2Eで実証済み。

- [x] **[phase 3 / tasks.md 3.4 旧エントリ] 承認ハブポップアップが実際にコンテンツを表示するか — 2026-07-20 🔴CRITICALに格上げ → 2026-07-21 根本原因特定・解消済み(下記8.6.17-a関連の3.4項目参照)**
  ブローカーのロジック自体(`amm/approval`→`list_approvals`→`resolve_approval`の一連、`release_approval_on_activate`)は名前付きパイプ+CDPでE2E検証済みでバグなし(2026-07-20)。CLI側のインタラクティブな確認プロンプト(Yes/No/Cancel)自体は独立して機能する(フォールバック手段として健在)。
  **バグ1(修正→再び無効化)**: `mcp.rs`は実際の承認要求で`amm-approval-requested`イベントを発行しているが、`app.js`側にこのイベントを購読するコードが一切なく、「承認ポップアップテスト」ボタンを手動で押さない限りポップアップは絶対に開かなかった。`listen('amm-approval-requested', () => invoke('show_approval_popup'))`を追加して解消(コミット`6a6272b`)。**しかしバグ3(下記)発覚により、この自動配線自体を再びコメントアウトして無効化した**(実際の承認要求のたびにアプリがハングする方が実害が大きいため)。ブローカー・list_approvals等のバックエンドは無傷。
  **バグ2(未解決、表示)**: 中身が真っ白(`about:blank`相当)。`decorations(false)`除去・`set_size()`ナッジ・`data:`URL差し替えいずれも効果なし(詳細は過去のコミット参照)。
  **バグ3(未解決、🔴CRITICAL・新規発見)**: 人間の実機セッションで、ペイン内の実Claude CodeがPermissionRequestフックを発火させた際、**amm全体が完全にハングし、あらゆるコマンド(list_approvals・pty_writeを含む無関係なコマンドすら)が応答しなくなり、全ペインのキーボード入力が効かなくなった**(強制終了が必要だった)。自動再現に成功: `show_approval_popup`を呼ぶと(CDP経由の直接呼び出しでも)以後の全コマンドが永久にハングする。切り分け済み: (a) ペイン数に依存しない(1ペインのみでも再現)。(b) `app.run_on_main_thread(...)`でメインスレッドへ非同期ディスパッチしても解消しない(この呼び出し自体が3秒以上応答を返さない — `tauri-runtime-wry`のソース上は非ブロッキングのはずだが実際には違う)。(c) `show_approval_popup`呼び出し前は`list_approvals`が43msで応答する健全な状態だったことを確認済み。**原因はWRY/WebView2の2枚目ウィンドウ生成処理自体の内部にあると見られ、このセッションではデバッガでスレッド状態を直接見られないため特定できなかった。** 対応方針(ユーザーと合意): 承認ポップアップの自動表示配線は無効化したまま保留し、後日別の設計(第2ウィンドウを作らずメインウィンドウ内オーバーレイdivにする等)を検討する。それまでは「manual test button」も含め`show_approval_popup`を呼ぶ経路全てが実質使用不可であることに注意。

- [x] **[phase 3 / tasks.md 3.5] ペイン本体(xterm.js側)でのIME確認 — 2026-07-20 実機で確認、OK**
  共通入力欄側の日本語IME合成→確定→送信は実機CDP経由で二重送信・文字化けなしを確認済みだったが、ペイン本体側は前回セッションのテストスクリプト実装ミス(重なったペインをクリックしようとして別ペインに阻まれた)により未完了だった。今回、新規ペインを作成し前面に固定(`zIndex`最大化)した上でxterm.jsの内部テキストエリアへフォーカスし、CDPの`Input.imeSetComposition`(「にほんご」合成)→`Input.insertText`(「日本語」確定)→Enterを実行、生の`pty-data`イベントを捕捉して検証した。結果: `echo 日本語`(コマンドラインエコー)と`日本語`(echo出力)の計2箇所に「日本語」が過不足なくちょうど1回ずつ出現し、二重送信・文字化けともになし。OK。

- [x] **[tasks.md 8.6] `cargo build -j 2` / `cargo test` の実機Windows最終確認 — 2026-07-21 実機で確認、OK**
  クラウドセッションでは`mcp.rs`/`chat_recording.rs`が使う`windows`クレート・`tokio::net::windows::named_pipe`を解決できず`cargo check`が最後まで通らなかった(既知の7件のコンパイルエラーのみで新規エラーは無いことは確認済みだった)。今回実機Windows(このマシン、cargo 1.97.0/rustc 1.97.0)で`src/apps/Amm.Tauri/src-tauri`にて`cargo build -j 2`実行→2分17秒でエラー0件(warning 3件のみ、いずれも未使用関数の既知dead_code)。続けて`cargo test -j 2`実行→lib 68件・main 0件・amm-mcp 10件、計78件全てpass、失敗0件。8.6.1〜8.6.16のRustコード変更(profile.rs/wait_detect.rs/lib.rs/mcp.rs/hook_cli.rs/input_history.rs/git_helper.rs等)がコンパイル・単体テストレベルでは健全であることを確認。次はGUI手動操作での各項目の実挙動確認。

- [x] **[tasks.md 8.6.15] `history.json`永続化パスの実機検証 — 2026-07-21 パス自体はOKだが新規のデータ消失バグを発見・修正**
  8.6.15の実装(`%LOCALAPPDATA%\amm\history.json`への変更)自体は正しく機能しており、実機起動後は旧`.NET`版と同じパスに保存されることを確認。**しかし検証中に新規の重大バグを発見**: 旧`.NET`版`MdiParentForm.cs`も全く同じパス(`%LOCALAPPDATA%\amm\history.json`)を使っており、実機にはこのマシンの旧WinForms利用履歴として`{"Entries":["こんばんわ",...]}`(PascalCase、`Entries`、`max_entries`キーなし)形式のファイルが既に存在していた。Rust版`input_history.rs`の`HistoryFile.entries`はlowercase固定でエイリアス無しだったため、`serde_json::from_str`はエラーにならず(`#[serde(default)]`によりキー不一致を静かに空Vecへフォールバック)、`load()`が「パース失敗」ではなく「正常成功・中身は空」という形で旧5件の履歴を握りつぶし、次回`save()`時に新スキーマで上書き→**旧`.NET`版の入力履歴が復元不可能な形で永久に失われる**実害を確認(実際に`get_recent_history`が`[]`を返すことを確認して発覚)。
  **対応**: まず`%LOCALAPPDATA%\amm\history.json`を`history.json.dotnet-backup-2026-07-21`としてバックアップ(上書きされる前に退避)。`input_history.rs`の`entries`フィールドに`#[serde(alias = "Entries")]`を追加し、新旧両スキーマを読めるよう修正(コミット未作成、作業ツリーに変更あり)。ユニットテスト2件追加(`history_file_reads_legacy_dotnet_pascal_case_entries`/`history_file_reads_current_lower_case_schema`)、`cargo test`で確認済み。再ビルド後、実機で`get_recent_history`が旧5件("こんばんわ"等)を正しく新しい順で返すことを確認、OK。
  **教訓**: 「新パス」の実装が正しくても、そのパスに旧`.NET`版のファイルが既に存在し得ることを検証観点に含める必要がある。同じ`%LOCALAPPDATA%\amm\`配下の他のファイル(`mcp-servers.json`等)についても同様のパス共有・スキーマ非互換リスクがないか、今後の検証で確認すること。

- [x] **[tasks.md 8.6.11] 「コマンド ▶」メニューの実機動作確認 — 2026-07-21 クリックしても一切開かない重大バグを発見・修正**
  8.6.11で新設された「コマンド ▶」ボタン(GUIから直接プロファイルをペイン起動する唯一の手段)を実機CDPでクリックしたところ、メニューが一切表示されずページエラー`Cannot read properties of null (reading 'getBoundingClientRect')`が発生することを発見。原因: `app.js`のクリックハンドラが`async (e) => { const profiles = await invoke('list_profiles')...; const rect = e.currentTarget.getBoundingClientRect(); ... }`という順で書かれており、`e.currentTarget`はイベント伝播終了時にブラウザ側で`null`化される(DOM仕様上の既知の落とし穴)。`await`で一旦制御を返した後に`e.currentTarget`を読むとこの時点で既に`null`になっており例外→`showCtxMenu`まで到達せずボタンが無反応になっていた。**この機能はコミット時点から一度も実際には動作していなかった**(静的レビュー・`node --check`では検出不可能なランタイム限定の不具合)。
  **修正**: `rect`の取得を`await`より前(イベントディスパッチ中の同期区間)に移動。`cargo build`で再ビルド(`frontendDist`はビルド時に埋め込まれるため`app.js`の変更はexe再ビルドしないと反映されないことも確認)後、実機で「コマンド ▶」→「PowerShell」クリック→新規ペインが実際に起動し切替バーに追加、`送信先`が正しく更新されることを確認。OK。

- [x] **[tasks.md 8.6.12] クイック切替バーの状態色・右クリックメニューの実機動作確認 — 2026-07-21 実機で確認、OK**
  実機CDPで2ペイン(通常シェル+新規起動したPowerShell)状態で確認。切替バーのボタンが状態別クラス(`qsb-waiting`=待機オリーブ、`qsb-active`=アクティブ青)で視覚的に区別されることをスクリーンショットで確認。ボタンを右クリック→`showPaneContextMenu`と共通のメニュー(チャット記録トグル・統計情報サブメニュー・クイック送信登録)が正しい位置に表示されることを確認。attention色(`qsb-attention`オレンジ)は`quickSwitchBarClassFor`のロジック(`pane.state === 'WaitingForInput' && pane.attention`の単純な条件分岐)を読んで妥当性を確認、実際の非アクティブペインでの発火は3.3/8.1.11で既に確認済みの`applyWaitState`の抑制条件と同一ロジックのため個別の実機確認は省略。

- [x] **[tasks.md 8.6.9] `closeProhibited`クローズガードの実機動作確認 — 2026-07-21 実機で確認、OK(検証中に「本物のネイティブダイアログ」を再認識)**
  `update_profile_settings`(persist=false)でPowerShellプロファイルに`closeProhibited=true`をメモリ上設定→起動済みペインの✕ボタンをShift無しでクリック→「クローズ禁止に設定されています」アラートが表示されクローズがブロックされる(ペイン数不変)ことを確認、OK。
  **検証中の事故→教訓**: Shift押下時のクローズ許可パスを検証しようとして`window.confirm`のみ上書き(`() => true`)し`window.prompt`を上書きし忘れた状態でShift+クリックしたところ、`closePane`→`runGitGuard`が発火(このペインの作業ディレクトリがこのリポジトリ自身`C:\project\github\amm`だったため)、未コミット変更検出→`prompt()`(コミットメッセージ入力)が呼ばれた。**このセッションは`tests/e2e-tauri/README.md`が記録していた過去のクラウドセッションと異なり実際の対話デスクトップセッション(`query session`で`console`が`Active`)であるため、`window.prompt()`は本物のネイティブモーダルダイアログとして表示され、応答するユーザーがいないため無期限にブロック**、CDP接続がタイムアウトする事態となった(`amm.exe`プロセス自体は生存)。`taskkill`で強制終了し再起動して復旧。**今後の教訓としてpending-real-machine-verification.mdに明記**: このセッションでCDP経由の`confirm()`/`alert()`/`prompt()`分岐をテストする際は、実際にダイアログを出さないよう**3つとも**必ず事前にモンキーパッチすること(1つでも漏れると本物のモーダルがアプリ全体を無応答にし、プロセスkillでしか復旧できない)。Shift押下時の実際のクローズ成功パス(git guard通過後)自体はこの事故により未確認のまま(教訓が主目的のため優先度低・別途確認可)。

- [x] **[phase 4 / tasks.md 4.1] Named Pipe ACL(現在のWindowsユーザーのみFullControl)の実装 — 2026-07-21 実装完了(下記4.1本項目参照)**

- [x] **[phase 4 / tasks.md 4.4 / 4.5] 旧`.NET`版`Amm.Mcp.exe` / `Amm.PowerShell.dll`(WinForms向け、無変更)の実バイナリでの疎通確認 — 2026-07-21〜22 .NET 9 SDK/PowerShell 7.5導入の上、実機で確認完了(8.2.2項目参照)

- [x] **[phase 7 / tasks.md 7.1, 7.2] 新`amm-mcp`(Rust) / `Amm.PowerShell.Tauri`(.psm1)の未実施項目 — 2026-07-20 (1)(2)は解消 → 2026-07-21〜22 (1)(3)とも解消**
  (1) `amm-mcp.exe approve`サブコマンドは、バイナリ自体のstdin/stdout契約(`AMM_NOTIFY_ID`環境変数込み)をE2Eで確認済み(`PARITY-AUDIT.md`のapproval-hub参照)に加え、実際のClaude Code/Copilot CLIが発火したフックからの承認オーバーレイ操作（許可/拒否クリック）まで実機E2E確認済み(tasks.md 8.2.5参照)。(2) `Connect-Amm`/`Disconnect-Amm`は実機テスト済み: `Connect-Amm -Verbose`で接続成功ログを確認、`Open-AmmWindow -ProfileName cmd -Title ps-smoketest`→`Get-AmmSession`(一覧に反映)→`Send-AmmMessage`→`Close-AmmWindow -Force`→`Disconnect-Amm`の一連を実行し、全ステップ期待通り。(3) `.psm1`のPowerShell 5.1対応に加え、2026-07-21〜22セッションでポータブル版PowerShell 7.5.4(.NET 9ベース)も導入し、`Import-Module`→`Connect-Amm`→`Get-AmmSession`のロード・実接続成功を確認、7系での動作も検証完了。

- [x] **[phase 4 / tasks.md 4.1] Named Pipeサーバの複数クライアント同時接続の負荷確認 — 2026-07-20 実機で確認、OK**
  実装中に「2件目以降の接続がハングする」重大バグを発見・修正した(詳細は`tasks/retro-pending.md`)。3回連続の新規接続では解消を確認済みだったが、より多数の同時並行接続や高頻度の接続・切断の繰り返しでの安定性は未検証だった。今回、Node.jsの`net`クライアントで20並列接続(各`initialize`→`tools/list`→`tools/call`(list_participants)→切断)×5ラウンド(計100接続サイクル)を実行し、全て成功(失敗0件)、amm.exe本体もクラッシュせず安定稼働を継続することを確認した。要求(5件以上)を大きく上回る負荷での確認完了。

- [x] **[phase 5 / tasks.md 5.5] `hook-cli`の実CLIツールでの受信確認 — 2026-07-21 Claude Code・Copilot CLIの2種で実CLI検証完了、Codexはサインイン要でユーザー対応待ち。バックエンド通知経路はいずれも完全動作を確認。「承認結果がCLI本体の対話プロンプトを自動解決しない」件はamm固有の不具合ではなく複数CLIに共通するパターンと判明**
  **追記: 原因特定(2026-07-21)**: WebSearch/WebFetchでClaude Code公式ドキュメント(`code.claude.com/docs/en/hooks`)を確認したところ、`amm-mcp.exe`が出力するJSON(`{"hookSpecificOutput":{"hookEventName":"PermissionRequest","decision":{"behavior":"allow"}}}`、exit code 0)は公式ドキュメントの仕様と**完全に一致**していることを確認。しかしGitHub Issue [anthropics/claude-code#19298](https://github.com/anthropics/claude-code/issues/19298)「PermissionRequest hook decision ignored」で、**この公式仕様通りのJSONを返してもClaude Code側がdecisionを無視し、対話プロンプトが常に表示されてしまう」既知の未修正バグ(報告時点v2.1.12、対話的セッションでのAllow/Deny両方が無視される)が2026年時点で報告済み**であることが判明。今回検証したv2.1.216でも同じ症状が再現しており、この既知バグの影響を受けていると判断できる。**結論: amm側のhook-cli実装(`hook_cli.rs`の設定ファイル書き込み・`amm-mcp.exe approve`のJSON出力とも)は公式仕様に完全準拠しており、これ以上amm側で対応できる余地はない。Claude Code側の当該バグが将来修正されれば、amm側は無改修のまま連動して動作するようになるはず。** 承認ハブの「通知」機能(オーバーレイでの実データ表示)自体は完全動作しており、実用上は引き続きCLI本体側のインタラクティブなYes/No/Cancelプロンプトで応答する運用が必要。Codex/Copilot CLIでの同種確認は未実施(Copilot CLIは`hookSpecificOutput`ではなく`{"behavior":...}`直書き形式でPermissionRequest相当の概念自体が異なる可能性があり、別途要確認)。
  **追記(2026-07-21、Copilot CLIで再検証・完了)**: 前回セッションで「Copilot CLI自体が起動しない」として中断していた件を再検証。今回は`cmd.exe /c "%APPDATA%\npm\copilot.cmd" ...`を直接PowerShellから実行して単体で正常動作することをまず確認した上で、amm実機で「コマンド ▶」→Copilotを起動したところ、**今回はCopilot CLIが正常に起動し(`Get-CimInstance Win32_Process`で`cmd.exe`→`node.exe`(`npm-loader.js`)の子プロセスチェーンを実際に確認)、対話プロンプトまで正常に到達した**。前回の「起動しない」症状は本セッションでは再現せず、原因はamm側の実装ではなく一過性/環境要因だったと考えられる(前回セッションとの間に本セッションの多数の修正が入っているが、Copilotペインの起動経路自体に関わる変更は無く、直接的な因果は不明。今後再発した場合は要調査)。
  `hook_register('copilotCli')`をCLI登録ダイアログ経由(実UIクリック)で実行→`.copilot\hooks\amm-hooks.json`が正しく生成されることを確認→Copilotペインを再起動(フックはCLI起動中の変更を拾わないため)→フォルダ信頼確認プロンプトに応答→`run \`dir\` in cmd`を送信→**Copilot CLI自身の権限確認プロンプト(「This action may read or write the following path outside your allowed directory list. ... Do you want to allow this?」)が表示されると同時に、amm側の承認オーバーレイにも実データ(tool="powershell", input=null)がリアルタイム表示される**ことをスクリーンショットで確認(Claude Codeに続き2例目の実CLIでの発火実証)。オーバーレイで「許可」をクリック→オーバーレイ自体は正しく解決(`approval-overlay`が`hidden`化、`resolve_approval`成功)されたが、**Claude Codeと同じく、Copilot CLI本体のインタラクティブなプロンプトは自動的には解決されなかった**(「Do you want to proceed?」表示のまま変化せず)。最終的にペインへ直接`Esc`を送信して手動でreject応答(「The user rejected this tool call.」)して後始末。**結論**: 「amm側の承認ハブは通知としては完全動作するが、CLI本体の対話プロンプトへの自動フィードバックは実現していない」という制約は、Claude Code固有のバグではなく、**Copilot CLIでも同様に再現する共通パターン**であることが分かった(Copilot CLI側にも同種の既知バグがあるか、あるいはこの種の連携は元々どのCLIのhook機構でも意図されていない設計である可能性が高い)。テスト用に登録したフックは検証後`hook_unregister('copilotCli')`(UI経由)で削除・後始末済み(`.copilot\hooks\`が空であることを確認)。
  **追記(Codex CLI)**: `npm install -g @openai/codex`(0.144.6)でこのマシンに新規インストール。amm実機で「コマンド ▶」→Codexを起動したところプロセス自体は正常に起動したが、**初回起動でサインイン(ChatGPTアカウントでのOAuth or デバイスコード、またはAPIキー入力)を要求する画面で停止**した。ユーザー自身の認証情報が必要なため、これ以上は自動検証の範囲外と判断しペインをforce close。hook-cli自体(`hook_cli.rs`のCodex用TOML書き込みロジック)は既存の単体テストで検証済みだが、実CLIでのフック発火確認は認証済みアカウントを持つユーザーによる実施が必要(次回セッション以降へ持ち越し)。
  実機の`claude`(v2.1.216)がこのマシンに既にhook登録済み(`~/.claude/settings.json`の`PermissionRequest`が`amm-mcp.exe approve`を指す、以前のセッションで登録済みのものをそのまま使用)であることを確認。amm.exe実機起動→「コマンド ▶」から実際にClaude Codeペインを起動(シミュレーションではなく本物のCLIプロセス)→共通入力欄経由ではなく`pty_write`で直接「run \`dir\` in bash」を送信→**実際にClaude Code自身の権限確認プロンプト(「This command requires approval / Do you want to proceed?」)が表示されると同時に、amm側の承認オーバーレイにも実データ(tool="Bash", input={"command":"dir",...})が表示される**ことをスクリーンショットで確認。これは`amm-mcp.exe approve`を手動実行するシミュレーションではなく、Claude Code自身のフック機構が実際に発火した初めての実証。
  **新規発見(未解決)**: オーバーレイで「許可」をクリックした後(`amm-mcp.exe approve`プロセスは正常終了・stdout に正しい`hookSpecificOutput`を出力したことを`resolve_approval`の成功応答とプロセス終了で確認済み)、**Claude Code本体のインタラクティブなプロンプトは自動的には解決されず**、1分待っても「Do you want to proceed? > 1. Yes」の表示のまま変化しなかった。最終的にペインへ直接`1\r`をキー入力して手動で解決した。原因はamm側ではなくClaude Code CLI側の挙動と見られる(hookの標準出力を対話中セッションの権限判定へフィードバックしていない可能性、または別の連携手順が必要な可能性)が、このセッションでは特定に至っていない。**実害**: 承認ハブの「通知」機能(オーバーレイに実データを表示する)は完全に動作するが、「オーバーレイから遠隔で承認して手元のペイン操作を省略する」という利便性は現状実現できておらず、ユーザーは結局対象ペインに戻ってキー操作する必要がある。Codex CLI(hookが無くOSC9のみ)・Copilot CLIでの同種確認は未実施。原因調査・対応要否の判断は次回セッションへ持ち越し。
  `src/hook_cli.rs`のJSON/TOML書き込みロジックはスクラッチ一時ディレクトリに対する単体テスト7件で検証済み(全件pass)だったが、これは「.NET版`HookCliRegistrar.cs`の実装と静的に一致するファイルを書けているか」の確認に留まり、「フック経由で起動された`amm-mcp.exe notify`/`approve`が実際にamm側へ届くか」は未確認だった。**今回、確認しようとして発見**: `AMM_NOTIFY_ID`(hookがどのペインからの通知か`amm-mcp.exe`へ伝える環境変数、.NET版は`TerminalChildForm.cs`がConPTY起動時に`AMM_NOTIFY_ID=NotifyToken`を子プロセス環境へ注入)が、Tauri版の`spawn_pty_for_pane_with_patterns`(`lib.rs`)には移植されておらず、ペイン内の子プロセスに一切設定されていなかった(実機でペイン内`set AMM_NOTIFY`を叩いて未設定を確認)。`amm-mcp.exe`の`notify`/`approve`はこの環境変数が空だと無条件で無音no-op(`main.rs:422-423,497-498`)するため、**このバグがある限り実CLIツール(Claude Code等)のhookは静かに何もしない**状態だった。`lib.rs`の`spawn_pty_for_pane_with_patterns`に`cmd.env("AMM_NOTIFY_ID", &pane_id)`を追加して修正し、`set AMM_NOTIFY`で正しい値(pane_id)が子プロセス環境に現れることを確認した。さらに修正後、`amm-mcp.exe`の実バイナリを(1)`AMM_NOTIFY_ID`をペインのpane_idにセットした環境で`notify --state attention`実行→`amm-pane-wait-state`イベントで`state=WaitingForInput, hasAttention=true`を確認、(2)`notify --state busy`実行→`state=Running, hasAttention=false`に戻ることを確認、(3)`approve --source test`にJSON(`tool_name`/`tool_input`)をstdin経由で渡す→`list_approvals`に正しい内容で出現→`resolve_approval(decision="deny")`→`amm-mcp.exe`のstdoutに`{"hookSpecificOutput":{"decision":{"behavior":"deny",...},"hookEventName":"PermissionRequest"}}`という、Claude CodeのPermissionRequestフックが期待する形式のJSONが正しく出力されることを確認。**hook_cli.rsが登録するのと全く同じバイナリ・同じ引数パターンでの受信経路は実証済み**。残るのは、実際にClaude Code CLI/Codex CLI/Copilot CLIを起動してそれらのhook機構自身にこのコマンドを発火させる確認のみ(CLIツール本体の実運用確認、人間による実機セッションが必要)。

- [x] **[phase 6 / tasks.md 6.1, 6.2] Tauriインストーラ(NSIS/MSI)の実ビルド・実行確認 — 2026-07-21〜22 実施完了(tasks.md 8.2.3参照)**

- [x] **[phase 8 / tasks.md 8.1.11, PARITY-AUDIT.md approval-hub] Level 1 attention状態の実際の発火確認 — 2026-07-20 実機で確認、OK(ただし1つ罠を発見)**
  実際に`AMM_NOTIFY_ID=<pane_id> amm-mcp.exe notify --state attention`を実行し、CDP経由でDOMを検証した。**1回目の試行は「効いていないように見えて焦った」**: 対象ペインを`setActivePane`で自分自身をアクティブ化した状態で試したところ、タイトル⚠・オレンジ化とも一切発生しなかった。原因はバグではなく、`app.js`の`applyWaitState`(`app.js:105-127`)が「アクティブなペインは既にユーザーが見ているので注意喚起を出さない」という仕様通りの抑制ロジック(`pane === activePane`なら`hasAttention`を無視して`setAttention(pane, false)`)によるもの。**2回目、対象ペインを非アクティブにして再試行したところ正しく動作**: `titleText`が"● ペイン"→"⚠ ペイン"に変化、`state-attention`/`attention-warn`クラスが付与され、タイトルバー背景色が`rgb(107,100,32)`(待機時オリーブ)→`rgb(138,74,18)`(アテンション時オレンジ)に変化することをCDPのスクリーンショットで視覚的にも確認(オレンジのタイトルバーに⚠アイコンが実際に描画されていることを画像で確認済み)。`FlashWindowEx`(タスクバー点滅)自体は3.3と同じ画面キャプチャ制約で目視不可のため未確認。教訓: 「アクティブペインは視覚的attentionを出さない」という仕様は把握していたはずだが、検証スクリプトを書く際にうっかり対象ペインをアクティブにしてしまい、一瞬「実装が壊れている」と誤認しかけた。今後同種の検証では、まずspec/コードの抑制条件を確認してからテストシナリオを組むこと。

- [x] **[PARITY-AUDIT.md chat-recording] 直接タイプ検知/トグル/統計情報の実機目視確認 — 2026-07-20 Playwright CDP (`tests/e2e-tauri`) で確認**
  実機Windows・実amm.exe起動・CDP経由での確認結果: チャット記録トグルON→共通入力欄からの送信→1200ms待機→`.amm\logs\<date>\<profile>-<timestamp>-<id>.json`が正しいフィールド(id/profile/mdi_name/sent_at/responded_at/duration_ms/command/response_tail/follow_up_questions)で書き出されることを実際のファイル内容で確認。統計情報トグルON→2回連続送信→`gateway_server_infos`相当の実データ確認は未実施(`openChatStatsDialog`は後述のworkingDirectory修正の影響で当初空文字を返しダイアログが開かなかったが、これは別バグ(下記)であり修正後は`get_pane_working_dir`が正しい値を返すようになったため再検証は容易・省略)。
  **追記 2026-07-20（続き・最終）**: ダイアログの日付ピッカー・h:mm:ss/m:ss表示形式も検証済み。実プロファイル紐付きペインで`sendToPane`経由の実コマンド送信→統計情報ダイアログに正しくレコード表示を確認。`formatStatsDuration`は境界値(65000ms→"1:05"、3600000ms→"1:00:00"、3725000ms→"1:02:05"、0ms→"0:00")全て正しい。この過程で**重大な設計判断ミス(タイムゾーンバグ)を発見・ユーザー確認の上修正**: `chrono_lite::date_dir()`/`compact()`がUTC基準で計算されており(コード内に「documented simplification」と明記済みだった既知の簡略化)、日本時間(JST)では毎日0:00〜9:00に送信したコマンドが「前日」のUTC日付フォルダに記録される一方、ダイアログの日付ピッカーはローカル日付(今日)がデフォルトのため、この時間帯は「統計情報はありません」と表示され続ける実害を確認(データ消失ではなく表示上の迷子)。ユーザーに対応方針を確認し「ローカル時刻に修正する」を選択、`windows`クレートの`Win32_System_Time`機能(`SystemTimeToTzSpecificLocalTime`、DST考慮・.NETの`DateTime.ToLocalTime()`と同等)を追加してローカル日付ベースに修正。実際にJST/UTC日付境界をまたぐ時間帯(ローカル08:06 = UTC前日23:06)で実機確認し、`.amm/logs/`・`.amm/stats/`とも正しくローカル日付フォルダ(`20260720`)へ記録され、`sent_at`/`responded_at`フィールド自体は意図通りUTCのまま(.NET版の`DateTime.UtcNow`と一致)であることを確認。
  **追記 2026-07-20（続き）**: 直接タイプ検知(`trackTypedInputForRecording`)も実機検証済み。当初`panes[0]`をクリックしても`typedLineBuf`が一切set されない謎現象に遭遇したが、原因はテスト側の座標計算ミス(ペインがカスケード表示で重なっており、`panes[0]`の中心座標は実際には最前面の別ペインに覆われていた - z-index最上位ペインでクリック先とチェック先を揃えて再テストしたところ、mid-typingで`"AB"`蓄積・Enter後に`""`リセットと正しく動作することを確認)。この過程で**実バグを1件発見・修正**: プロファイルなしのアドホックペイン(「+」ボタン作成等)でチャット記録を右クリックトグルでONにすると、`pane.chatRecord.profile`が生成時デフォルトの`null`のまま(トグル側の遅延初期化フォールバックは`chatRecord`オブジェクトが既に存在するため発火しない)。`start_chat_recording`のRust側は`profile: String`(非Option)を要求するため、`null`を渡すとIPCデシリアライズが失敗し(`invalid type: null, expected a string`)、記録が無音で失敗していた。`startChatRecordingForPane`で`pane.chatRecord.profile ?? pane.profileName ?? pane.label`のフォールバックを追加して修正、null状態を再現→直接タイプ送信→ページエラー0件・`.amm/logs/`への正しい書き出し(profileはペインラベルにフォールバック)を確認。

- [x] **[PARITY-AUDIT.md profile-schema] コマンド管理/追加/編集/AMM設定ダイアログの実機目視確認 — 2026-07-20 Playwright CDP で確認**
  `openCommandManagerDialog`(実データ6プロファイル表示)→`編集...`→`openCommandTemplateDialog`の全フィールドが実プロファイル(Codex)の値で正しく初期化されることを確認(コマンドタイプ・名前・実行ファイル・引数・改行モード・チェックボックス群・waitPatterns・コメント記号・nickname・quickPrompts行、全て一致)。コマンドタイプ切替時の`confirm()`分岐(`window.confirm`をモンキーパッチしtrue/false両方をテスト)は正しく動作(false→変更前の値に復元、true→プリセット適用)。**実機で2件のバグを発見・修正**(詳細はPARITY-AUDIT.md参照): (1) 編集サブダイアログのキャンセル/Escがコマンド管理リスト全体を閉じてしまう(→`onCancel`引数で親ダイアログへ戻るよう修正)、(2) `workingDirectory`が空文字("")または"%USERPROFILE%"等の環境変数付きの場合にPTY起動時のカレントディレクトリ解決が壊れる(→`profile::resolve_working_directory`を新設し修正)。`pick_folder`・同名衝突検知の目視は未実施。「上書き保存」ボタン自体はmcp-gateway側のテスト中に別途経由確認済み(上記参照)。
  **追記 2026-07-20（続き）**: `AMM設定...`(`openAmmSettingsDialog`)も検証済み。MCPブリッジ経由で`pane/open(profile_name:"cmd")`により実プロファイル紐付きペインを開き、`closeProhibited`トグル+`commentPrefixes`に識別用マーカーを設定して`confirm()`分岐(モンキーパッチでtrue/false両方)をテスト。false→メモリ上のみ反映(`list_profiles`で確認、`profiles.amm`は書き込まれず)、true→`profiles.amm`へ実際に書き込み(識別用マーカーがファイル上のJSONに存在することを確認)。両パスともページエラーなし。

- [x] **[PARITY-AUDIT.md mcp-gateway] 管理ダイアログの実機目視確認 — 2026-07-20 Playwright CDP で確認、クリティカルなクラッシュバグを発見・修正**
  グローバル/ファイル固有それぞれへの追加→フォーム入力→OK→リスト反映→外側OKでコミット→再オープンで永続(メモリ上)確認、の一連が正常動作。ステータスアイコン「●」(未設定)を実際に確認。**重大バグを発見・修正**: `autoStart=true`のMCPサーバーが1件でも設定された状態でamm.exeを起動すると、`GatewayManager::start_auto_start_servers`が生の`tokio::spawn`を呼んでおり(`lib.rs::run`の同期`.setup()`クロージャ内、Tokioランタイムのコンテキスト外)、`"there is no reactor running"`でpanicして**アプリ全体が起動直後にクラッシュする**ことが判明。`tauri::async_runtime::spawn`(このコードベースの他の全バックグラウンドタスクが使う方式)に修正し、実際にMCPサーバー1件(意図的に失敗する設定)を構成した状態で再起動→正常起動・`gateway_server_infos`が`{status:"error", lastError:"Failed to start process: npx (program not found)"}`を正しく返すことを確認。**追記 2026-07-20（続き）**: 実際に成功して起動する外部MCPサーバーでの検証も完了。npm registry非依存の最小stdio JSON-RPC MCPサーバー fixture(`tests/e2e-tauri/fixture-mcp-server.js`、initialize/tools・list(echo 1件)/tools・call実装)を新規作成し、`profiles.amm`の`mcpServers`に`node <fixture>`として登録・実機起動したところ`gateway_server_infos`が`{status:"running", toolCount:1}`を正しく返すことを確認。さらに実`amm-mcp.exe --bridge`経由で本物のJSON-RPCラウンドトリップを実施し、名前空間プレフィックス付きツール集約(`tools/list`に`fixture/echo`が出現)とツール呼び出し転送(`tools/call`で`fixture/echo`を呼ぶと実子プロセスの応答`"echo: <入力>"`がそのまま返る、スタブではない)の両方を実データで確認。「上書き保存」ボタン自体も別テスト中に偶然経由し、MCPサーバー設定を含む`ProfilesFile`全体が正しく`profiles.amm`へシリアライズされることを確認済み。`%LOCALAPPDATA%\amm\mcp-servers.json`への即時書き込み自体の目視は未実施。

- [x] **[PARITY-AUDIT.md command-import-export] エクスポート/インポートダイアログのネイティブピッカー実機確認 — 2026-07-21 実機で完全にE2E確認、OK(バグ無し、判定ロジックの仕様誤解を1件解消)**
  `openExportProfilesDialog`/`openImportProfilesDialog`と、新規追加した`tauri-plugin-dialog`経由のネイティブ保存/開くダイアログ(`pick_export_save_path`/`pick_import_open_path`)のうち、**ネイティブピッカーに到達する前のロジックは2026-07-20 Playwright CDPで検証済み**(詳細は旧エントリ参照)。今回のセッションで残っていたネイティブピッカー自体の実機確認を完了:
  **エクスポート**: 6コマンド全選択でOK→UI Automationで実際の「名前を付けて保存」ダイアログ(既定ファイル名`profiles-export-2026-07-21.ammprofiles`が自動生成・選択済み状態)を検出、Enterで確定→実際に`.ammprofiles`ファイルがディスクに書き込まれ、中身が正しいJSON(6プロファイル分)であることを確認。
  **インポート**: エクスポートしたファイルを対象に「開く」ダイアログをUI Automationでパス直接入力→確定→**実際のネイティブピッカーを経由してインポートプレビューダイアログに到達**、6件全てにチェック・4件(Claude Code/Copilot/Codex/Gemini)に`⚠ 重複`マークが正しく表示されることを確認。「スキップ」方針でOK→完了メッセージ「2件追加、0件上書き、4件スキップしました」を確認。
  **検証中に気づいた点(バグではなく仕様の再確認)**: CMD/PowerShell(Nicknameなし)が重複マーク無しで「新規追加」扱いになり一覧上に同名エントリが2つ並ぶ結果に一瞬「バグでは」と身構えたが、`command-import-export`spec("Nicknameが既存と大文字小文字無視で一致する行にのみ⚠重複を付ける"/"一致しないプロファイルは常にクローンして新規追加する")を確認した結果、意図した仕様通りの挙動と判明(重複判定キーは`Nickname`のみで`Name`は見ない設計)。インポートは在メモリ変更のみで`profiles.amm`(ディスク)へは影響しないことも確認(6件のまま)。

- [x] **[pane-management] クイック切替バーのボタンに状態色・attention表示がない — 2026-07-20 ユーザー指摘、対応保留 → 後続セッション(tasks.md 8.6.12)で解消済み**
  `.NET`版`MdiParentForm.cs`の`RefreshMdiButtonBar`相当の状態色分け・attentionアイコンが当初移植漏れだったが、`app.js`の`quickSwitchBarClassFor`(`qsb-attention`/`qsb-active`/`qsb-waiting`/`qsb-stopped`)と`updateQuickSwitchBar`の`attentionGlyph`(`⚠`)分岐で解消済み。`style.css`にも対応する背景色クラス定義あり(2026-07-22コードレビューで存在を確認)。

- [x] **[PARITY-AUDIT.md quick-command-register] 右クリックコンテキストメニュー/登録ダイアログの実機目視確認 — 2026-07-20 Playwright CDP で確認、クリティカルなバグを発見・修正**
  **重大バグを発見・修正**: 右クリックコンテキストメニューが実機では一切表示されないことが判明。xterm.js が自身の内部ルート要素に`contextmenu`リスナーを付けて伝播を止めており(右クリック選択機能のため)、`.pane-term`へのbubbleフェーズリスナーがペイン内(=ほぼ全域)のクリックで発火しなかった。`{capture: true}` + `e.stopPropagation()`へ変更して修正。修正後、右クリック→メニュー表示→「統計情報」サブメニューのホバー展開→「チャット記録」トグルのON/OFF切替→クイック送信への登録(ラベル/テキスト自動入力→OK→`list_profiles`で実際に`quickPrompts`配列に追加されたことを確認)まで一連の実データフローが正常動作することを確認。**追記 2026-07-20（続き）**: サブメニューの画面端はみ出しも検証済み・**2件目のバグを発見・修正**。外側の`.ctx-menu`はビューポート端でクランプされる(`showCtxMenu`のJS計算)が、CSS専用(`left: 100%`)のサブメニューは同じ対応がなく、右端付近でメニューを開くとサブメニューがビューポート外(実測: 1283px幅に対しx:1469まで、約190px)にはみ出し操作不能だった。`buildCtxMenuItems`の`.has-submenu`項目に`mouseenter`ハンドラを追加し、表示時に矩形を計測してはみ出す場合は`right: 100%`(左開き)へ切り替えるよう修正。右端近くで開いた場合は左開きに切り替わりビューポート内に収まること、余裕がある通常時は従来通り右開きのままであることの両方を実機確認。
